[package]
name = "libdisc"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Raw optical disc model for the QEMU CD backend: formats, subchannel, C2, CD-DA (doc 05)"

[lib]
crate-type = ["rlib", "staticlib"]

[dependencies]
