[package]
name = "win98xp-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "libretro core: in-process QEMU for Win98/XP vintage machines (M0: test-pattern shell)"

[lib]
name = "win98xp_libretro"
crate-type = ["cdylib"]

[dependencies]
