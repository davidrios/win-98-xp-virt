//! Companion launcher stub (doc 07). Real work starts in M6; this exists so
//! the workspace, CI, and packaging shape are complete from M0.

fn main() {
    println!(
        "win98-xp-virt launcher {} — nothing to launch yet (see docs/08-roadmap.md, M6)",
        env!("CARGO_PKG_VERSION")
    );
}
