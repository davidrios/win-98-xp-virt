SSE inline fast path: scalar and packed float on the host FPU (doc 16).

Every SSE float instruction was a helper call into softfloat (hardfloat
once PE is sticky, but still a call and a lane loop). This patch
translates the common cases inline when MXCSR admits the host FPU
verbatim (RC nearest, no FTZ/DAZ, all exceptions masked, property
sse-fast on) AND the precision flag is already sticky in env->sse_status
(TB flag bit 31): then an admissible operation can raise nothing but PE,
so the inline code only classifies its result (operands for compares)
and takes the unmodified helper out of line otherwise, bit- and
flag-exact against softfloat. ldmxcsr/fxrstor/xrstor end the TB; a TB
translated before PE was sticky exits after the helper that sets it.

TCG grows scalar binary32 opcodes (add/sub/mul/div/sqrt_f32 on i32
temps in vector registers), int32 <-> float conversions, and vector
float opcodes (fadd/fsub/fmul/fdiv/fsqrt_vec, MO_32/MO_64) in the
aarch64 and x86-64 backends. Packed instructions run on the vector unit
with vector checks and one lane-mask branch; scalar ones in general
registers. Inlined: add, sub, mul, div, min, max, sqrt (ps/pd/ss/sd),
rcp, rsqrt (ps/ss), cmp with the 8 legacy predicates, comis/ucomis,
cvt(t)ss2si, cvt(t)sd2si, cvtsi2ss/sd, cvtss2sd, cvtsd2ss. Legacy
encodings only. Slow blocks share x87-shadow's array and emitter.

Test: tools/sse-guest-test.py (DOS program, 61 sequences x every pair
of 47 single / 33 double edge values x 3 MXCSR settings, 333,875 result
lines identical on/off on the M1 Air). Register-only bench: packed 12x,
scalar 3.6x over the helpers. x86-64 backend code is untested (no host).
--- a/include/tcg/tcg-opc.h
+++ b/include/tcg/tcg-opc.h
@@ -201,6 +201,26 @@
 DEF(sqrt_f64, 1, 1, 0, IMPL64 | IMPL(TCG_TARGET_HAS_f64))
 DEF(cvt_f64_f32, 1, 1, 0, IMPL64 | IMPL(TCG_TARGET_HAS_f64))
 DEF(cvt_f32_f64, 1, 1, 0, IMPL64 | IMPL(TCG_TARGET_HAS_f64))
+/*
+ * Scalar IEEE binary32 on the host FPU, raw bit patterns in i32 temps
+ * (target/i386 sse-fast.c.inc); same rules as the f64 set.
+ */
+DEF(add_f32, 1, 2, 0, IMPL(TCG_TARGET_HAS_f64))
+DEF(sub_f32, 1, 2, 0, IMPL(TCG_TARGET_HAS_f64))
+DEF(mul_f32, 1, 2, 0, IMPL(TCG_TARGET_HAS_f64))
+DEF(div_f32, 1, 2, 0, IMPL(TCG_TARGET_HAS_f64))
+DEF(sqrt_f32, 1, 1, 0, IMPL(TCG_TARGET_HAS_f64))
+/*
+ * int32 <-> binary32/binary64. cvt_i32_*: round to nearest even.
+ * cvt_*_i32 rounds to nearest even, cvtt_*_i32 truncates; NaN and
+ * out-of-range inputs give a host-defined integer.
+ */
+DEF(cvt_i32_f32, 1, 1, 0, IMPL(TCG_TARGET_HAS_f64))
+DEF(cvt_i32_f64, 1, 1, 0, IMPL64 | IMPL(TCG_TARGET_HAS_f64))
+DEF(cvt_f32_i32, 1, 1, 0, IMPL(TCG_TARGET_HAS_f64))
+DEF(cvtt_f32_i32, 1, 1, 0, IMPL(TCG_TARGET_HAS_f64))
+DEF(cvt_f64_i32, 1, 1, 0, IMPL(TCG_TARGET_HAS_f64))
+DEF(cvtt_f64_i32, 1, 1, 0, IMPL(TCG_TARGET_HAS_f64))
 
 #define DATA64_ARGS  (TCG_TARGET_REG_BITS == 64 ? 1 : 2)
 
@@ -313,6 +333,16 @@
 DEF(bitsel_vec, 1, 3, 0, IMPLVEC | IMPL(TCG_TARGET_HAS_bitsel_vec))
 DEF(cmpsel_vec, 1, 4, 1, IMPLVEC | IMPL(TCG_TARGET_HAS_cmpsel_vec))
 
+/*
+ * IEEE binary32/binary64 lanes (vece MO_32 / MO_64) on the host vector
+ * unit, round to nearest even, flags unobservable (target/i386 sse-fast).
+ */
+DEF(fadd_vec, 1, 2, 0, IMPLVEC | IMPL(TCG_TARGET_HAS_fp_vec))
+DEF(fsub_vec, 1, 2, 0, IMPLVEC | IMPL(TCG_TARGET_HAS_fp_vec))
+DEF(fmul_vec, 1, 2, 0, IMPLVEC | IMPL(TCG_TARGET_HAS_fp_vec))
+DEF(fdiv_vec, 1, 2, 0, IMPLVEC | IMPL(TCG_TARGET_HAS_fp_vec))
+DEF(fsqrt_vec, 1, 1, 0, IMPLVEC | IMPL(TCG_TARGET_HAS_fp_vec))
+
 DEF(last_generic, 0, 0, 0, TCG_OPF_NOT_PRESENT)
 
 #if TCG_TARGET_MAYBE_vec
--- a/include/tcg/tcg.h
+++ b/include/tcg/tcg.h
@@ -156,6 +156,7 @@
 #define TCG_TARGET_HAS_bitsel_vec       0
 #define TCG_TARGET_HAS_cmpsel_vec       0
 #define TCG_TARGET_HAS_tst_vec          0
+#define TCG_TARGET_HAS_fp_vec           0
 #else
 #define TCG_TARGET_MAYBE_vec            1
 #endif
--- a/include/tcg/tcg-op-common.h
+++ b/include/tcg/tcg-op-common.h
@@ -253,6 +253,23 @@
 void tcg_gen_cvt_f64_f32(TCGv_i64 ret, TCGv_i64 a);
 /* binary32 in the low 32 bits of a (high bits ignored) -> binary64, exact */
 void tcg_gen_cvt_f32_f64(TCGv_i64 ret, TCGv_i64 a);
+/* Host-FPU binary32 ops on raw bit patterns in i32 temps; only when tcg_f64_supported(). */
+void tcg_gen_add_f32(TCGv_i32 ret, TCGv_i32 a, TCGv_i32 b);
+void tcg_gen_sub_f32(TCGv_i32 ret, TCGv_i32 a, TCGv_i32 b);
+void tcg_gen_mul_f32(TCGv_i32 ret, TCGv_i32 a, TCGv_i32 b);
+void tcg_gen_div_f32(TCGv_i32 ret, TCGv_i32 a, TCGv_i32 b);
+void tcg_gen_sqrt_f32(TCGv_i32 ret, TCGv_i32 a);
+/* int32 -> binary32 / binary64, round to nearest even */
+void tcg_gen_cvt_i32_f32(TCGv_i32 ret, TCGv_i32 a);
+void tcg_gen_cvt_i32_f64(TCGv_i64 ret, TCGv_i32 a);
+/*
+ * binary32 / binary64 -> int32, round to nearest even (cvt) or toward
+ * zero (cvtt); the caller guarantees a finite value in range.
+ */
+void tcg_gen_cvt_f32_i32(TCGv_i32 ret, TCGv_i32 a);
+void tcg_gen_cvtt_f32_i32(TCGv_i32 ret, TCGv_i32 a);
+void tcg_gen_cvt_f64_i32(TCGv_i32 ret, TCGv_i64 a);
+void tcg_gen_cvtt_f64_i32(TCGv_i32 ret, TCGv_i64 a);
 void tcg_gen_mulsu2_i64(TCGv_i64 rl, TCGv_i64 rh, TCGv_i64 arg1, TCGv_i64 arg2);
 void tcg_gen_not_i64(TCGv_i64 ret, TCGv_i64 arg);
 void tcg_gen_ext8s_i64(TCGv_i64 ret, TCGv_i64 arg);
@@ -474,6 +491,13 @@
 void tcg_gen_cmpsel_vec(TCGCond cond, unsigned vece, TCGv_vec r,
                         TCGv_vec a, TCGv_vec b, TCGv_vec c, TCGv_vec d);
 
+/* host float lanes (fadd_vec ..): only when tcg_fp_vec_supported(vece) */
+bool tcg_fp_vec_supported(unsigned vece);
+void tcg_gen_fadd_vec(unsigned vece, TCGv_vec r, TCGv_vec a, TCGv_vec b);
+void tcg_gen_fsub_vec(unsigned vece, TCGv_vec r, TCGv_vec a, TCGv_vec b);
+void tcg_gen_fmul_vec(unsigned vece, TCGv_vec r, TCGv_vec a, TCGv_vec b);
+void tcg_gen_fdiv_vec(unsigned vece, TCGv_vec r, TCGv_vec a, TCGv_vec b);
+void tcg_gen_fsqrt_vec(unsigned vece, TCGv_vec r, TCGv_vec a);
 void tcg_gen_ld_vec(TCGv_vec r, TCGv_ptr base, TCGArg offset);
 void tcg_gen_st_vec(TCGv_vec r, TCGv_ptr base, TCGArg offset);
 void tcg_gen_stl_vec(TCGv_vec r, TCGv_ptr base, TCGArg offset, TCGType t);
--- a/tcg/tcg-op.c
+++ b/tcg/tcg-op.c
@@ -3117,8 +3117,74 @@
 {
     tcg_debug_assert(tcg_f64_supported());
     tcg_gen_op2_i64(INDEX_op_cvt_f32_f64, ret, a);
+}
+
+void tcg_gen_add_f32(TCGv_i32 ret, TCGv_i32 a, TCGv_i32 b)
+{
+    tcg_debug_assert(tcg_f64_supported());
+    tcg_gen_op3_i32(INDEX_op_add_f32, ret, a, b);
+}
+
+void tcg_gen_sub_f32(TCGv_i32 ret, TCGv_i32 a, TCGv_i32 b)
+{
+    tcg_debug_assert(tcg_f64_supported());
+    tcg_gen_op3_i32(INDEX_op_sub_f32, ret, a, b);
+}
+
+void tcg_gen_mul_f32(TCGv_i32 ret, TCGv_i32 a, TCGv_i32 b)
+{
+    tcg_debug_assert(tcg_f64_supported());
+    tcg_gen_op3_i32(INDEX_op_mul_f32, ret, a, b);
+}
+
+void tcg_gen_div_f32(TCGv_i32 ret, TCGv_i32 a, TCGv_i32 b)
+{
+    tcg_debug_assert(tcg_f64_supported());
+    tcg_gen_op3_i32(INDEX_op_div_f32, ret, a, b);
+}
+
+void tcg_gen_sqrt_f32(TCGv_i32 ret, TCGv_i32 a)
+{
+    tcg_debug_assert(tcg_f64_supported());
+    tcg_gen_op2_i32(INDEX_op_sqrt_f32, ret, a);
+}
+
+void tcg_gen_cvt_i32_f32(TCGv_i32 ret, TCGv_i32 a)
+{
+    tcg_debug_assert(tcg_f64_supported());
+    tcg_gen_op2_i32(INDEX_op_cvt_i32_f32, ret, a);
+}
+
+void tcg_gen_cvt_i32_f64(TCGv_i64 ret, TCGv_i32 a)
+{
+    tcg_debug_assert(tcg_f64_supported());
+    tcg_gen_op2(INDEX_op_cvt_i32_f64, tcgv_i64_arg(ret), tcgv_i32_arg(a));
+}
+
+void tcg_gen_cvt_f32_i32(TCGv_i32 ret, TCGv_i32 a)
+{
+    tcg_debug_assert(tcg_f64_supported());
+    tcg_gen_op2_i32(INDEX_op_cvt_f32_i32, ret, a);
+}
+
+void tcg_gen_cvtt_f32_i32(TCGv_i32 ret, TCGv_i32 a)
+{
+    tcg_debug_assert(tcg_f64_supported());
+    tcg_gen_op2_i32(INDEX_op_cvtt_f32_i32, ret, a);
 }
 
+void tcg_gen_cvt_f64_i32(TCGv_i32 ret, TCGv_i64 a)
+{
+    tcg_debug_assert(tcg_f64_supported());
+    tcg_gen_op2(INDEX_op_cvt_f64_i32, tcgv_i32_arg(ret), tcgv_i64_arg(a));
+}
+
+void tcg_gen_cvtt_f64_i32(TCGv_i32 ret, TCGv_i64 a)
+{
+    tcg_debug_assert(tcg_f64_supported());
+    tcg_gen_op2(INDEX_op_cvtt_f64_i32, tcgv_i32_arg(ret), tcgv_i64_arg(a));
+}
+
 void tcg_gen_muls2_i64(TCGv_i64 rl, TCGv_i64 rh, TCGv_i64 arg1, TCGv_i64 arg2)
 {
     if (TCG_TARGET_HAS_muls2_i64) {
--- a/tcg/tcg-op-vec.c
+++ b/tcg/tcg-op-vec.c
@@ -584,8 +584,45 @@
 void tcg_gen_add_vec(unsigned vece, TCGv_vec r, TCGv_vec a, TCGv_vec b)
 {
     do_op3_nofail(vece, r, a, b, INDEX_op_add_vec);
+}
+
+bool tcg_fp_vec_supported(unsigned vece)
+{
+    return TCG_TARGET_HAS_fp_vec
+        && tcg_can_emit_vec_op(INDEX_op_fadd_vec, TCG_TYPE_V128, vece) > 0;
+}
+
+void tcg_gen_fadd_vec(unsigned vece, TCGv_vec r, TCGv_vec a, TCGv_vec b)
+{
+    do_op3_nofail(vece, r, a, b, INDEX_op_fadd_vec);
 }
 
+void tcg_gen_fsub_vec(unsigned vece, TCGv_vec r, TCGv_vec a, TCGv_vec b)
+{
+    do_op3_nofail(vece, r, a, b, INDEX_op_fsub_vec);
+}
+
+void tcg_gen_fmul_vec(unsigned vece, TCGv_vec r, TCGv_vec a, TCGv_vec b)
+{
+    do_op3_nofail(vece, r, a, b, INDEX_op_fmul_vec);
+}
+
+void tcg_gen_fdiv_vec(unsigned vece, TCGv_vec r, TCGv_vec a, TCGv_vec b)
+{
+    do_op3_nofail(vece, r, a, b, INDEX_op_fdiv_vec);
+}
+
+void tcg_gen_fsqrt_vec(unsigned vece, TCGv_vec r, TCGv_vec a)
+{
+    TCGTemp *rt = tcgv_vec_temp(r);
+    TCGTemp *at = tcgv_vec_temp(a);
+    TCGType type = rt->base_type;
+
+    tcg_debug_assert(at->base_type >= type);
+    tcg_debug_assert(tcg_can_emit_vec_op(INDEX_op_fsqrt_vec, type, vece) > 0);
+    vec_gen_2(INDEX_op_fsqrt_vec, type, vece, temp_arg(rt), temp_arg(at));
+}
+
 void tcg_gen_sub_vec(unsigned vece, TCGv_vec r, TCGv_vec a, TCGv_vec b)
 {
     do_op3_nofail(vece, r, a, b, INDEX_op_sub_vec);
--- a/tcg/tcg.c
+++ b/tcg/tcg.c
@@ -2172,6 +2172,17 @@
     case INDEX_op_sqrt_f64:
     case INDEX_op_cvt_f64_f32:
     case INDEX_op_cvt_f32_f64:
+    case INDEX_op_add_f32:
+    case INDEX_op_sub_f32:
+    case INDEX_op_mul_f32:
+    case INDEX_op_div_f32:
+    case INDEX_op_sqrt_f32:
+    case INDEX_op_cvt_i32_f32:
+    case INDEX_op_cvt_i32_f64:
+    case INDEX_op_cvt_f32_i32:
+    case INDEX_op_cvtt_f32_i32:
+    case INDEX_op_cvt_f64_i32:
+    case INDEX_op_cvtt_f64_i32:
         return TCG_TARGET_HAS_f64;
 
     case INDEX_op_mov_vec:
@@ -2179,6 +2190,12 @@
     case INDEX_op_dupm_vec:
     case INDEX_op_ld_vec:
     case INDEX_op_st_vec:
+    case INDEX_op_fadd_vec:
+    case INDEX_op_fsub_vec:
+    case INDEX_op_fmul_vec:
+    case INDEX_op_fdiv_vec:
+    case INDEX_op_fsqrt_vec:
+        return have_vec && TCG_TARGET_HAS_fp_vec;
     case INDEX_op_add_vec:
     case INDEX_op_sub_vec:
     case INDEX_op_and_vec:
--- a/tcg/aarch64/tcg-target.h
+++ b/tcg/aarch64/tcg-target.h
@@ -169,6 +169,7 @@
 #define TCG_TARGET_HAS_bitsel_vec       1
 #define TCG_TARGET_HAS_cmpsel_vec       0
 #define TCG_TARGET_HAS_tst_vec          1
+#define TCG_TARGET_HAS_fp_vec           1
 
 #define TCG_TARGET_DEFAULT_MO (0)
 #define TCG_TARGET_NEED_LDST_LABELS
--- a/tcg/aarch64/tcg-target-con-set.h
+++ b/tcg/aarch64/tcg-target-con-set.h
@@ -15,6 +15,7 @@
 C_O0_I2(w, r)
 C_O0_I3(rZ, rZ, r)
 C_O1_I1(r, r)
+C_O1_I1(r, w)
 C_O1_I1(w, r)
 C_O1_I1(w, w)
 C_O1_I1(w, wr)
--- a/tcg/aarch64/tcg-target.c.inc
+++ b/tcg/aarch64/tcg-target.c.inc
@@ -533,6 +533,26 @@
     I3F_FSQRTD      = 0x1e61c000,
     I3F_FCVTSD      = 0x1e624000,   /* Sd = Dn */
     I3F_FCVTDS      = 0x1e22c000,   /* Dd = Sn */
+
+    /* Scalar binary32 FP and int32 conversions (sse-fast.c.inc). */
+    I3F_FADDS       = 0x1e202800,
+    I3F_FSUBS       = 0x1e203800,
+    I3F_FMULS       = 0x1e200800,
+    I3F_FDIVS       = 0x1e201800,
+    I3F_FSQRTS      = 0x1e21c000,
+    I3F_SCVTFS      = 0x1e220000,   /* Sd = Wn */
+    I3F_SCVTFD      = 0x1e620000,   /* Dd = Wn */
+    I3F_FCVTNSS     = 0x1e200000,   /* Wd = Sn, round to nearest even */
+    I3F_FCVTNSD     = 0x1e600000,   /* Wd = Dn */
+    I3F_FCVTZSS     = 0x1e380000,   /* Wd = Sn, round toward zero */
+    I3F_FCVTZSD     = 0x1e780000,   /* Wd = Dn */
+
+    /* Vector FP, sz (bit 22) selects S/D lanes, Q (bit 30) the width. */
+    I3FV_FADD       = 0x0e20d400,
+    I3FV_FSUB       = 0x0ea0d400,
+    I3FV_FMUL       = 0x2e20dc00,
+    I3FV_FDIV       = 0x2e20fc00,
+    I3FV_FSQRT      = 0x2ea1f800,
 
     /* Logical shifted register instructions (without a shift).  */
     I3510_AND       = 0x0a000000,
@@ -820,6 +840,13 @@
                         TCGReg rd, TCGReg rn, TCGReg rm)
 {
     tcg_out32(s, insn | (rm & 0x1f) << 16 | (rn & 0x1f) << 5 | (rd & 0x1f));
+}
+
+static void tcg_out_fpv(TCGContext *s, AArch64Insn insn, bool q,
+                        unsigned vece, TCGReg rd, TCGReg rn, TCGReg rm)
+{
+    tcg_out32(s, insn | q << 30 | (vece == MO_64) << 22 | (rm & 0x1f) << 16
+              | (rn & 0x1f) << 5 | (rd & 0x1f));
 }
 
 static void tcg_out_fp4(TCGContext *s, AArch64Insn insn,
@@ -2546,7 +2573,41 @@
         break;
     case INDEX_op_cvt_f32_f64:
         tcg_out32(s, I3F_FCVTDS | (a1 & 0x1f) << 5 | (a0 & 0x1f));
+        break;
+
+    case INDEX_op_add_f32:
+        tcg_out_fp3(s, I3F_FADDS, a0, a1, a2);
+        break;
+    case INDEX_op_sub_f32:
+        tcg_out_fp3(s, I3F_FSUBS, a0, a1, a2);
+        break;
+    case INDEX_op_mul_f32:
+        tcg_out_fp3(s, I3F_FMULS, a0, a1, a2);
+        break;
+    case INDEX_op_div_f32:
+        tcg_out_fp3(s, I3F_FDIVS, a0, a1, a2);
+        break;
+    case INDEX_op_sqrt_f32:
+        tcg_out32(s, I3F_FSQRTS | (a1 & 0x1f) << 5 | (a0 & 0x1f));
+        break;
+    case INDEX_op_cvt_i32_f32:
+        tcg_out32(s, I3F_SCVTFS | (a1 & 0x1f) << 5 | (a0 & 0x1f));
+        break;
+    case INDEX_op_cvt_i32_f64:
+        tcg_out32(s, I3F_SCVTFD | (a1 & 0x1f) << 5 | (a0 & 0x1f));
+        break;
+    case INDEX_op_cvt_f32_i32:
+        tcg_out32(s, I3F_FCVTNSS | (a1 & 0x1f) << 5 | (a0 & 0x1f));
+        break;
+    case INDEX_op_cvtt_f32_i32:
+        tcg_out32(s, I3F_FCVTZSS | (a1 & 0x1f) << 5 | (a0 & 0x1f));
         break;
+    case INDEX_op_cvt_f64_i32:
+        tcg_out32(s, I3F_FCVTNSD | (a1 & 0x1f) << 5 | (a0 & 0x1f));
+        break;
+    case INDEX_op_cvtt_f64_i32:
+        tcg_out32(s, I3F_FCVTZSD | (a1 & 0x1f) << 5 | (a0 & 0x1f));
+        break;
 
     case INDEX_op_mb:
         tcg_out_mb(s, a0);
@@ -2631,6 +2692,21 @@
     case INDEX_op_dupm_vec:
         tcg_out_dupm_vec(s, type, vece, a0, a1, a2);
         break;
+    case INDEX_op_fadd_vec:
+        tcg_out_fpv(s, I3FV_FADD, is_q, vece, a0, a1, a2);
+        break;
+    case INDEX_op_fsub_vec:
+        tcg_out_fpv(s, I3FV_FSUB, is_q, vece, a0, a1, a2);
+        break;
+    case INDEX_op_fmul_vec:
+        tcg_out_fpv(s, I3FV_FMUL, is_q, vece, a0, a1, a2);
+        break;
+    case INDEX_op_fdiv_vec:
+        tcg_out_fpv(s, I3FV_FDIV, is_q, vece, a0, a1, a2);
+        break;
+    case INDEX_op_fsqrt_vec:
+        tcg_out_fpv(s, I3FV_FSQRT, is_q, vece, a0, a1, 0);
+        break;
     case INDEX_op_add_vec:
         if (is_scalar) {
             tcg_out_insn(s, 3611, ADD, vece, a0, a1, a2);
@@ -2940,6 +3016,13 @@
     case INDEX_op_umax_vec:
     case INDEX_op_umin_vec:
         return vece < MO_64;
+    case INDEX_op_fadd_vec:
+    case INDEX_op_fsub_vec:
+    case INDEX_op_fmul_vec:
+    case INDEX_op_fdiv_vec:
+    case INDEX_op_fsqrt_vec:
+        /* S lanes in either width, D lanes only as .2D */
+        return vece == MO_32 || (vece == MO_64 && type == TCG_TYPE_V128);
 
     default:
         return 0;
@@ -3033,7 +3116,28 @@
     case INDEX_op_sqrt_f64:
     case INDEX_op_cvt_f64_f32:
     case INDEX_op_cvt_f32_f64:
+    case INDEX_op_sqrt_f32:
         return C_O1_I1(w, w);
+    case INDEX_op_add_f32:
+    case INDEX_op_sub_f32:
+    case INDEX_op_mul_f32:
+    case INDEX_op_div_f32:
+        return C_O1_I2(w, w, w);
+    case INDEX_op_cvt_i32_f32:
+    case INDEX_op_cvt_i32_f64:
+        return C_O1_I1(w, r);
+    case INDEX_op_cvt_f32_i32:
+    case INDEX_op_cvtt_f32_i32:
+    case INDEX_op_cvt_f64_i32:
+    case INDEX_op_cvtt_f64_i32:
+        return C_O1_I1(r, w);
+    case INDEX_op_fadd_vec:
+    case INDEX_op_fsub_vec:
+    case INDEX_op_fmul_vec:
+    case INDEX_op_fdiv_vec:
+        return C_O1_I2(w, w, w);
+    case INDEX_op_fsqrt_vec:
+        return C_O1_I1(w, w);
 
     case INDEX_op_ld8u_i32:
     case INDEX_op_ld8s_i32:
--- a/tcg/i386/tcg-target.h
+++ b/tcg/i386/tcg-target.h
@@ -228,6 +228,7 @@
 #define TCG_TARGET_HAS_bitsel_vec       have_avx512vl
 #define TCG_TARGET_HAS_cmpsel_vec       1
 #define TCG_TARGET_HAS_tst_vec          have_avx512bw
+#define TCG_TARGET_HAS_fp_vec           have_avx1
 
 #define TCG_TARGET_deposit_i32_valid(ofs, len) \
     (((ofs) == 0 && ((len) == 8 || (len) == 16)) || \
--- a/tcg/i386/tcg-target-con-set.h
+++ b/tcg/i386/tcg-target-con-set.h
@@ -31,6 +31,7 @@
 C_O1_I1(r, L)
 C_O1_I1(r, q)
 C_O1_I1(r, r)
+C_O1_I1(r, x)
 C_O1_I1(x, r)
 C_O1_I1(x, x)
 C_O1_I2(q, 0, qi)
--- a/tcg/i386/tcg-target.c.inc
+++ b/tcg/i386/tcg-target.c.inc
@@ -419,6 +419,27 @@
 #define OPC_VSQRTSD     (0x51 | P_EXT | P_SIMDF2)
 #define OPC_VCVTSD2SS   (0x5a | P_EXT | P_SIMDF2)
 #define OPC_VCVTSS2SD   (0x5a | P_EXT | P_SIMDF3)
+#define OPC_VADDSS      (0x58 | P_EXT | P_SIMDF3)
+#define OPC_VSUBSS      (0x5c | P_EXT | P_SIMDF3)
+#define OPC_VMULSS      (0x59 | P_EXT | P_SIMDF3)
+#define OPC_VDIVSS      (0x5e | P_EXT | P_SIMDF3)
+#define OPC_VSQRTSS     (0x51 | P_EXT | P_SIMDF3)
+#define OPC_VCVTSI2SS   (0x2a | P_EXT | P_SIMDF3)
+#define OPC_VCVTSI2SD   (0x2a | P_EXT | P_SIMDF2)
+#define OPC_VCVTSS2SI   (0x2d | P_EXT | P_SIMDF3)
+#define OPC_VCVTSD2SI   (0x2d | P_EXT | P_SIMDF2)
+#define OPC_VCVTTSS2SI  (0x2c | P_EXT | P_SIMDF3)
+#define OPC_VCVTTSD2SI  (0x2c | P_EXT | P_SIMDF2)
+#define OPC_VADDPS      (0x58 | P_EXT)
+#define OPC_VADDPD      (0x58 | P_EXT | P_DATA16)
+#define OPC_VSUBPS      (0x5c | P_EXT)
+#define OPC_VSUBPD      (0x5c | P_EXT | P_DATA16)
+#define OPC_VMULPS      (0x59 | P_EXT)
+#define OPC_VMULPD      (0x59 | P_EXT | P_DATA16)
+#define OPC_VDIVPS      (0x5e | P_EXT)
+#define OPC_VDIVPD      (0x5e | P_EXT | P_DATA16)
+#define OPC_VSQRTPS     (0x51 | P_EXT)
+#define OPC_VSQRTPD     (0x51 | P_EXT | P_DATA16)
 #define OPC_VPBLENDD    (0x02 | P_EXT3A | P_DATA16)
 #define OPC_VPBLENDVB   (0x4c | P_EXT3A | P_DATA16)
 #define OPC_VPBLENDMB   (0x66 | P_EXT38 | P_DATA16 | P_EVEX)
@@ -3073,6 +3094,41 @@
         break;
     case INDEX_op_cvt_f32_f64:
         tcg_out_vex_modrm(s, OPC_VCVTSS2SD, a0, a0, a1);
+        break;
+
+    case INDEX_op_add_f32:
+        tcg_out_vex_modrm(s, OPC_VADDSS, a0, a1, a2);
+        break;
+    case INDEX_op_sub_f32:
+        tcg_out_vex_modrm(s, OPC_VSUBSS, a0, a1, a2);
+        break;
+    case INDEX_op_mul_f32:
+        tcg_out_vex_modrm(s, OPC_VMULSS, a0, a1, a2);
+        break;
+    case INDEX_op_div_f32:
+        tcg_out_vex_modrm(s, OPC_VDIVSS, a0, a1, a2);
+        break;
+    case INDEX_op_sqrt_f32:
+        tcg_out_vex_modrm(s, OPC_VSQRTSS, a0, a0, a1);
+        break;
+    case INDEX_op_cvt_i32_f32:
+        /* a1 is a general register (constraint r) in the r/m field */
+        tcg_out_vex_modrm(s, OPC_VCVTSI2SS, a0, a0, a1);
+        break;
+    case INDEX_op_cvt_i32_f64:
+        tcg_out_vex_modrm(s, OPC_VCVTSI2SD, a0, a0, a1);
+        break;
+    case INDEX_op_cvt_f32_i32:
+        tcg_out_vex_modrm(s, OPC_VCVTSS2SI, a0, 0, a1);
+        break;
+    case INDEX_op_cvtt_f32_i32:
+        tcg_out_vex_modrm(s, OPC_VCVTTSS2SI, a0, 0, a1);
+        break;
+    case INDEX_op_cvt_f64_i32:
+        tcg_out_vex_modrm(s, OPC_VCVTSD2SI, a0, 0, a1);
+        break;
+    case INDEX_op_cvtt_f64_i32:
+        tcg_out_vex_modrm(s, OPC_VCVTTSD2SI, a0, 0, a1);
         break;
 
     case INDEX_op_mb:
@@ -3504,6 +3560,23 @@
 #endif
     case INDEX_op_abs_vec:
         insn = abs_insn[vece];
+        a2 = a1;
+        a1 = 0;
+        goto gen_simd;
+    case INDEX_op_fadd_vec:
+        insn = vece == MO_64 ? OPC_VADDPD : OPC_VADDPS;
+        goto gen_simd;
+    case INDEX_op_fsub_vec:
+        insn = vece == MO_64 ? OPC_VSUBPD : OPC_VSUBPS;
+        goto gen_simd;
+    case INDEX_op_fmul_vec:
+        insn = vece == MO_64 ? OPC_VMULPD : OPC_VMULPS;
+        goto gen_simd;
+    case INDEX_op_fdiv_vec:
+        insn = vece == MO_64 ? OPC_VDIVPD : OPC_VDIVPS;
+        goto gen_simd;
+    case INDEX_op_fsqrt_vec:
+        insn = vece == MO_64 ? OPC_VSQRTPD : OPC_VSQRTPS;
         a2 = a1;
         a1 = 0;
         goto gen_simd;
@@ -3660,7 +3733,28 @@
     case INDEX_op_sqrt_f64:
     case INDEX_op_cvt_f64_f32:
     case INDEX_op_cvt_f32_f64:
+    case INDEX_op_sqrt_f32:
         return C_O1_I1(x, x);
+    case INDEX_op_add_f32:
+    case INDEX_op_sub_f32:
+    case INDEX_op_mul_f32:
+    case INDEX_op_div_f32:
+        return C_O1_I2(x, x, x);
+    case INDEX_op_cvt_i32_f32:
+    case INDEX_op_cvt_i32_f64:
+        return C_O1_I1(x, r);
+    case INDEX_op_cvt_f32_i32:
+    case INDEX_op_cvtt_f32_i32:
+    case INDEX_op_cvt_f64_i32:
+    case INDEX_op_cvtt_f64_i32:
+        return C_O1_I1(r, x);
+    case INDEX_op_fadd_vec:
+    case INDEX_op_fsub_vec:
+    case INDEX_op_fmul_vec:
+    case INDEX_op_fdiv_vec:
+        return C_O1_I2(x, x, x);
+    case INDEX_op_fsqrt_vec:
+        return C_O1_I1(x, x);
 
     case INDEX_op_ld8u_i32:
     case INDEX_op_ld8u_i64:
@@ -3934,6 +4028,12 @@
     case INDEX_op_not_vec:
     case INDEX_op_bitsel_vec:
         return 1;
+    case INDEX_op_fadd_vec:
+    case INDEX_op_fsub_vec:
+    case INDEX_op_fmul_vec:
+    case INDEX_op_fdiv_vec:
+    case INDEX_op_fsqrt_vec:
+        return vece >= MO_32;
     case INDEX_op_cmp_vec:
     case INDEX_op_cmpsel_vec:
         return -1;
--- a/target/i386/cpu.h
+++ b/target/i386/cpu.h
@@ -1794,6 +1794,16 @@
      * 0 otherwise. Derived from fpuc by update_fp_status(), not migrated.
      */
     uint8_t x87_fast_mode;
+    /*
+     * SSE inline fast path (tcg/sse-fast.c.inc): 1 when MXCSR has RC
+     * nearest, no FTZ/DAZ, all exceptions masked and property sse-fast is
+     * on. Derived from mxcsr by update_mxcsr_status(), not migrated.
+     */
+    uint8_t sse_fast_mode;
+    /* tcg/sse-fast.c.inc: lane mask of the packed vector path */
+    uint64_t sses_scratch[2] QEMU_ALIGNED(16);
+    /* slow-path counters (info registers): guard, hand-over, helper, cvt/comis */
+    uint64_t sses_stat[4];
     /* shadow doubles of the x87 stack, relative slots (tcg/x87-shadow.c.inc) */
     uint64_t x87_sd[8];
 
@@ -2103,6 +2113,8 @@
     bool enforce_cpuid;
     /* x87 host-FPU fast path (target/i386/tcg/x87-fast.h) */
     bool x87_fast;
+    /* SSE host-FPU inline path (target/i386/tcg/sse-fast.c.inc) */
+    bool sse_fast;
     /*
      * Force features to be enabled even if the host doesn't support them.
      * This is dangerous and should be done only for testing CPUID
@@ -2544,6 +2556,9 @@
 /* TB flags (not hflags): x87 inline mode, env->x87_fast_mode (0, 1, 2) */
 #define TB_FLAG_X87_MODE_SHIFT  29
 #define TB_FLAG_X87_MODE_MASK   (3u << TB_FLAG_X87_MODE_SHIFT)
+/* SSE inline mode: sse_fast_mode and the precision flag already sticky */
+#define TB_FLAG_SSE_FAST_SHIFT  31
+#define TB_FLAG_SSE_FAST_MASK   (1u << TB_FLAG_SSE_FAST_SHIFT)
 
 static inline void cpu_get_tb_cpu_state(CPUX86State *env, vaddr *pc,
                                         uint64_t *cs_base, uint32_t *flags)
@@ -2551,6 +2566,10 @@
     *flags = env->hflags |
         (env->eflags & (IOPL_MASK | TF_MASK | RF_MASK | VM_MASK | AC_MASK)) |
         ((uint32_t)env->x87_fast_mode << TB_FLAG_X87_MODE_SHIFT);
+    if (env->sse_fast_mode
+        && (env->sse_status.float_exception_flags & float_flag_inexact)) {
+        *flags |= TB_FLAG_SSE_FAST_MASK;
+    }
     if (env->hflags & HF_CS64_MASK) {
         *cs_base = 0;
         *pc = env->eip;
--- a/target/i386/cpu.c
+++ b/target/i386/cpu.c
@@ -8496,6 +8496,7 @@
     DEFINE_PROP_BOOL("check", X86CPU, check_cpuid, true),
     DEFINE_PROP_BOOL("enforce", X86CPU, enforce_cpuid, false),
     DEFINE_PROP_BOOL("x87-fast", X86CPU, x87_fast, true),
+    DEFINE_PROP_BOOL("sse-fast", X86CPU, sse_fast, true),
     DEFINE_PROP_BOOL("x-force-features", X86CPU, force_features, false),
     DEFINE_PROP_BOOL("kvm", X86CPU, expose_kvm, true),
     DEFINE_PROP_UINT32("phys-bits", X86CPU, phys_bits, 0),
--- a/target/i386/cpu-dump.c
+++ b/target/i386/cpu-dump.c
@@ -496,6 +496,9 @@
                      env->fpstt,
                      fptag,
                      env->mxcsr);
+        qemu_fprintf(f, "SSE-fast slow paths: guard=%" PRIu64 " handover=%" PRIu64
+                     " helper=%" PRIu64 " cvt/comis=%" PRIu64 "\n",
+                     env->sses_stat[0], env->sses_stat[1], env->sses_stat[2], env->sses_stat[3]);
         for(i=0;i<8;i++) {
             CPU_LDoubleU u;
             u.d = env->fpregs[i].d;
--- a/target/i386/tcg/fpu_helper.c
+++ b/target/i386/tcg/fpu_helper.c
@@ -3482,6 +3482,11 @@
 
     /* set flush to zero */
     set_flush_to_zero((mxcsr & SSE_FZ) ? 1 : 0, &env->sse_status);
+
+    /* tcg/sse-fast.c.inc: the host FPU can run this MXCSR verbatim */
+    env->sse_fast_mode = env_archcpu(env)->sse_fast
+        && (mxcsr & (SSE_RC_MASK | SSE_DAZ | SSE_FZ)) == 0
+        && (mxcsr & 0x1f80) == 0x1f80;
 }
 
 void update_mxcsr_from_sse_status(CPUX86State *env)
--- a/target/i386/tcg/translate.c
+++ b/target/i386/tcg/translate.c
@@ -99,6 +99,11 @@
     bool cc_op_dirty;
     TCGv fdp_addr;
     int fdp_seg;
+    /* SSE inline path (sse-fast.c.inc) */
+    void *fn;                   /* helper to run out of line */
+    uint8_t nargs;
+    uint8_t reg_ot, reg_n;      /* integer destination (cvtss2si) */
+    int off[3];                 /* env offsets of the vector operands */
 } X87SlowBlock;
 #define X87S_MAX_SLOW 96
 #define X87S_MAX_TMPS 96
@@ -165,6 +170,11 @@
     TCGv x87_fdp_addr;          /* last x87 memory operand (FDP), or NULL */
     int x87_fdp_seg;
 
+    /* SSE inline state (sse-fast.c.inc); slow blocks share x87s_slow[] */
+    bool sses_on;               /* TB translated with TB_FLAG_SSE_FAST */
+    bool sses_guarded;          /* runtime guard emitted */
+    bool sses_pending;          /* hand-over check due at the end of the insn */
+
     /* TCG local temps */
     TCGv cc_srcT;
     TCGv A0;
@@ -299,6 +309,7 @@
 static void x87s_boundary(DisasContext *s);
 static void x87s_emit_slow_blocks(DisasContext *s);
 static void gen_x87_fip_fdp(DisasContext *s);
+static void sses_emit_slow_block(DisasContext *s, struct X87SlowBlock *b);
 static void gen_jmp_rel_csize(DisasContext *s, int diff, int tb_num);
 static void gen_exception_gpf(DisasContext *s);
 
@@ -2383,9 +2394,28 @@
     tcg_gen_ld_i128(t, tcg_env, offset + offsetof(YMMReg, YMM_X(1)));
     tcg_gen_qemu_st_i128(t, s->tmp0, mem_index, mop);
 }
+
+/* sse-fast.c.inc, used by emit.c.inc */
+static bool sses_arith(DisasContext *s, X86DecodedInsn *decode, int op,
+                       void *pd, void *ps, void *sd, void *ss);
+static bool sses_cmp(DisasContext *s, X86DecodedInsn *decode, void *fn);
+static bool sses_comis(DisasContext *s, X86DecodedInsn *decode, void *fn);
+static bool sses_cvtsi2s(DisasContext *s, X86DecodedInsn *decode, TCGv_i32 in);
+static bool sses_cvts2si(DisasContext *s, X86DecodedInsn *decode, bool trunc,
+                         void *ss2si, void *sd2si);
+static bool sses_cvt_ss_sd(DisasContext *s, X86DecodedInsn *decode,
+                           bool to_double, void *fn);
+static void sses_helper_done(DisasContext *s);
+static void sses_emit_pending(DisasContext *s);
+static void sses_end_tb(DisasContext *s);
+enum {
+    SSES_ADD, SSES_SUB, SSES_MUL, SSES_DIV, SSES_MIN, SSES_MAX,
+    SSES_SQRT, SSES_RCP, SSES_RSQRT, SSES_CMP,
+};
 
 #include "emit.c.inc"
 #include "x87-shadow.c.inc"
+#include "sse-fast.c.inc"
 
 static void gen_x87(DisasContext *s, X86DecodedInsn *decode)
 {
@@ -3762,6 +3792,10 @@
     dc->x87s_ntmps = 0;
     x87s_reset(dc);
     dc->x87_fdp_addr = NULL;
+    dc->sses_on = (flags & TB_FLAG_SSE_FAST_MASK) && tcg_f64_supported()
+        && !(flags & (HF_TF_MASK | HF_INHIBIT_IRQ_MASK));
+    dc->sses_guarded = false;
+    dc->sses_pending = false;
     /* select memory access functions */
     dc->mem_index = cpu_mmu_index(cpu, false);
     dc->cpuid_features = env->features[FEAT_1_EDX];
--- a/target/i386/tcg/decode-new.c.inc
+++ b/target/i386/tcg/decode-new.c.inc
@@ -2876,6 +2876,7 @@
         assert(!!decode.cc_src == !!(cc_live & USES_CC_SRC));
         assert(!!decode.cc_src2 == !!(cc_live & USES_CC_SRC2));
     }
+    sses_emit_pending(s);
 
     return;
  gp_fault:
--- a/target/i386/tcg/emit.c.inc
+++ b/target/i386/tcg/emit.c.inc
@@ -572,9 +572,16 @@
         fn(tcg_env, OP_PTR0, OP_PTR2);
     }
 }
-#define UNARY_FP_SSE(uname, lname)                                                 \
+#define UNARY_FP_SSE(uname, lname, sop)                                            \
 static void gen_##uname(DisasContext *s, X86DecodedInsn *decode)                   \
 {                                                                                  \
+    if (sses_arith(s, decode, sop,                                                 \
+                   (void *)gen_helper_##lname##pd_xmm,                             \
+                   (void *)gen_helper_##lname##ps_xmm,                             \
+                   (void *)gen_helper_##lname##sd,                                 \
+                   (void *)gen_helper_##lname##ss)) {                              \
+        return;                                                                    \
+    }                                                                              \
     gen_unary_fp_sse(s, decode,                                                    \
                      gen_helper_##lname##pd_xmm,                                   \
                      gen_helper_##lname##ps_xmm,                                   \
@@ -582,8 +589,9 @@
                      gen_helper_##lname##ps_ymm,                                   \
                      gen_helper_##lname##sd,                                       \
                      gen_helper_##lname##ss);                                      \
+    sses_helper_done(s);                                                           \
 }
-UNARY_FP_SSE(VSQRT, sqrt)
+UNARY_FP_SSE(VSQRT, sqrt, SSES_SQRT)
 
 /*
  * 00 = v*ps Vps, Hps, Wpd
@@ -611,9 +619,16 @@
     }
 }
 
-#define FP_SSE(uname, lname)                                                       \
+#define FP_SSE(uname, lname, sop)                                                  \
 static void gen_##uname(DisasContext *s, X86DecodedInsn *decode)                   \
 {                                                                                  \
+    if (sses_arith(s, decode, sop,                                                 \
+                   (void *)gen_helper_##lname##pd_xmm,                             \
+                   (void *)gen_helper_##lname##ps_xmm,                             \
+                   (void *)gen_helper_##lname##sd,                                 \
+                   (void *)gen_helper_##lname##ss)) {                              \
+        return;                                                                    \
+    }                                                                              \
     gen_fp_sse(s, decode,                                                          \
                gen_helper_##lname##pd_xmm,                                         \
                gen_helper_##lname##ps_xmm,                                         \
@@ -621,13 +636,14 @@
                gen_helper_##lname##ps_ymm,                                         \
                gen_helper_##lname##sd,                                             \
                gen_helper_##lname##ss);                                            \
+    sses_helper_done(s);                                                           \
 }
-FP_SSE(VADD, add)
-FP_SSE(VMUL, mul)
-FP_SSE(VSUB, sub)
-FP_SSE(VMIN, min)
-FP_SSE(VDIV, div)
-FP_SSE(VMAX, max)
+FP_SSE(VADD, add, SSES_ADD)
+FP_SSE(VMUL, mul, SSES_MUL)
+FP_SSE(VSUB, sub, SSES_SUB)
+FP_SSE(VMIN, min, SSES_MIN)
+FP_SSE(VDIV, div, SSES_DIV)
+FP_SSE(VMAX, max, SSES_MAX)
 
 #define FMA_SSE_PACKED(uname, ptr0, ptr1, ptr2, even, odd)                         \
 static void gen_##uname##Px(DisasContext *s, X86DecodedInsn *decode)               \
@@ -717,16 +733,20 @@
 illegal_op:
     gen_illegal_opcode(s);
 }
-#define UNARY_FP32_SSE(uname, lname)                                               \
+#define UNARY_FP32_SSE(uname, lname, sop)                                          \
 static void gen_##uname(DisasContext *s, X86DecodedInsn *decode)                   \
 {                                                                                  \
+    if (sses_arith(s, decode, sop, NULL, (void *)gen_helper_##lname##ps_xmm,       \
+                   NULL, (void *)gen_helper_##lname##ss)) {                        \
+        return;                                                                    \
+    }                                                                              \
     gen_unary_fp32_sse(s, decode,                                                  \
                        gen_helper_##lname##ps_xmm,                                 \
                        gen_helper_##lname##ps_ymm,                                 \
                        gen_helper_##lname##ss);                                    \
 }
-UNARY_FP32_SSE(VRSQRT, rsqrt)
-UNARY_FP32_SSE(VRCP, rcp)
+UNARY_FP32_SSE(VRSQRT, rsqrt, SSES_RSQRT)
+UNARY_FP32_SSE(VRCP, rcp, SSES_RCP)
 
 /*
  * 66 = v*pd Vpd, Hpd, Wpd
@@ -2013,8 +2033,8 @@
     } else {
         x87s_boundary(s);
         gen_helper_fxrstor(tcg_env, s->A0);
-        if (s->x87s_on) {
-            s->base.is_jmp = DISAS_EOB_NEXT;    /* control word may change */
+        if (s->x87s_on || tcg_f64_supported()) {
+            s->base.is_jmp = DISAS_EOB_NEXT;    /* control word / MXCSR may change */
         }
     }
 }
@@ -2360,6 +2380,7 @@
 {
     tcg_gen_trunc_tl_i32(s->tmp2_i32, s->T0);
     gen_helper_ldmxcsr(tcg_env, s->tmp2_i32);
+    sses_end_tb(s);
 }
 
 static void gen_lxx_seg(DisasContext *s, X86DecodedInsn *decode, int seg)
@@ -4229,6 +4250,9 @@
         s->prefix & PREFIX_REPNZ ? 3 /* sd */ :
         !!(s->prefix & PREFIX_DATA) /* pd */ + (s->vex_l << 2);
 
+    if (sses_cmp(s, decode, (void *)gen_helper_cmp_funcs[index][b])) {
+        return;
+    }
     gen_helper_cmp_funcs[index][b](tcg_env, OP_PTR0, OP_PTR1, OP_PTR2);
 }
 
@@ -4236,6 +4260,9 @@
 {
     SSEFunc_0_epp fn;
     fn = s->prefix & PREFIX_DATA ? gen_helper_comisd : gen_helper_comiss;
+    if (sses_comis(s, decode, (void *)fn)) {
+        return;
+    }
     fn(tcg_env, OP_PTR1, OP_PTR2);
     assume_cc_op(s, CC_OP_EFLAGS);
 }
@@ -4274,11 +4301,18 @@
 
 static void gen_VCVTSD2SS(DisasContext *s, X86DecodedInsn *decode)
 {
+    if (sses_cvt_ss_sd(s, decode, false, (void *)gen_helper_cvtsd2ss)) {
+        return;
+    }
     gen_helper_cvtsd2ss(tcg_env, OP_PTR0, OP_PTR1, OP_PTR2);
+    sses_helper_done(s);
 }
 
 static void gen_VCVTSS2SD(DisasContext *s, X86DecodedInsn *decode)
 {
+    if (sses_cvt_ss_sd(s, decode, true, (void *)gen_helper_cvtss2sd)) {
+        return;
+    }
     gen_helper_cvtss2sd(tcg_env, OP_PTR0, OP_PTR1, OP_PTR2);
 }
 
@@ -4305,11 +4339,15 @@
     in = s->T1;
 #endif
 
+    if (sses_cvtsi2s(s, decode, in)) {
+        return;
+    }
     if (s->prefix & PREFIX_REPNZ) {
         gen_helper_cvtsi2sd(tcg_env, OP_PTR0, in);
     } else {
         gen_helper_cvtsi2ss(tcg_env, OP_PTR0, in);
     }
+    sses_helper_done(s);
 }
 
 static inline void gen_VCVTtSx2SI(DisasContext *s, X86DecodedInsn *decode,
@@ -4333,6 +4371,10 @@
 #else
     out = s->T0;
 #endif
+    if (sses_cvts2si(s, decode, ss2si == gen_helper_cvttss2si,
+                     (void *)ss2si, (void *)sd2si)) {
+        return;
+    }
     if (s->prefix & PREFIX_REPNZ) {
         sd2si(out, tcg_env, OP_PTR2);
     } else {
@@ -4341,6 +4383,7 @@
 #ifdef TARGET_X86_64
     tcg_gen_extu_i32_tl(s->T0, out);
 #endif
+    sses_helper_done(s);
 }
 
 #ifndef TARGET_X86_64
@@ -4623,6 +4666,9 @@
 {
     SSEFunc_0_epp fn;
     fn = s->prefix & PREFIX_DATA ? gen_helper_ucomisd : gen_helper_ucomiss;
+    if (sses_comis(s, decode, (void *)fn)) {
+        return;
+    }
     fn(tcg_env, OP_PTR1, OP_PTR2);
     assume_cc_op(s, CC_OP_EFLAGS);
 }
@@ -4753,8 +4799,8 @@
     tcg_gen_concat_tl_i64(features, cpu_regs[R_EAX], cpu_regs[R_EDX]);
     x87s_boundary(s);
     gen_helper_xrstor(tcg_env, s->A0, features);
-    if (s->x87s_on) {
-        s->base.is_jmp = DISAS_EOB_NEXT;        /* control word may change */
+    if (s->x87s_on || tcg_f64_supported()) {
+        s->base.is_jmp = DISAS_EOB_NEXT;        /* control word / MXCSR may change */
     }
     if (s->cpuid_7_0_ebx_features & CPUID_7_0_EBX_MPX) {
         /*
--- a/target/i386/tcg/x87-shadow.c.inc
+++ b/target/i386/tcg/x87-shadow.c.inc
@@ -87,6 +87,13 @@
     X87S_SLOW_FSQRT,
     X87S_SLOW_FRNDINT,
     X87S_SLOW_FILD,
+    /* sse-fast.c.inc */
+    SSES_SLOW_FIRST,
+    SSES_SLOW_GUARD = SSES_SLOW_FIRST,
+    SSES_SLOW_EXIT_NEXT,
+    SSES_SLOW_HELPER,
+    SSES_SLOW_CVT2SI,
+    SSES_SLOW_COMIS,
 };
 
 QEMU_BUILD_BUG_ON(sizeof(FPReg) != 16);
@@ -703,6 +710,11 @@
             x87s_free_tmps(s);
             continue;
         }
+        if (b->kind >= SSES_SLOW_FIRST) {
+            sses_emit_slow_block(s, b);
+            x87s_free_tmps(s);
+            continue;
+        }
         x87s_flush_code(s);
         switch (b->kind) {
         case X87S_SLOW_ARITH_MEM:
--- /dev/null
+++ b/target/i386/tcg/sse-fast.c.inc
@@ -0,0 +1,658 @@
+/*
+ * SSE inline fast path: scalar and packed binary32/binary64 arithmetic,
+ * compares and int conversions on the host FPU, emitted inline by TCG
+ * (opcodes add_f32 .. cvtt_f64_i32, add_f64 .. of x87-shadow.c.inc).
+ * Doc 16 in the repository.
+ *
+ * Mode. A TB is translated in "SSE fast" mode (TB_FLAG_SSE_FAST) when
+ * MXCSR admits the host FPU verbatim (round to nearest, no FTZ/DAZ, all
+ * six exceptions masked, property sse-fast on: env->sse_fast_mode, kept
+ * by update_mxcsr_status) and the precision flag is already sticky in
+ * env->sse_status. That second condition is what makes the inline code
+ * flag-exact without a residual per lane: an admissible operation can
+ * raise nothing but PE, every other outcome is caught by a
+ * classification check on the result (on the operands for min/max and
+ * compares) and the instruction then runs the unmodified helper out of
+ * line. It is also the gate of softfloat's own hardfloat path.
+ *
+ * Whatever can change MXCSR or clear the flags (ldmxcsr, fxrstor, xrstor)
+ * ends the TB. A TB translated without the flag exits after the helper
+ * that first raises PE (sses_helper_done) so the next lookup finds the
+ * fast variant, and a runtime guard at the first inlined instruction of
+ * a fast TB re-executes it from a correctly translated block if the
+ * state does not match (it cannot, unless a clearing site was missed).
+ *
+ * Fast conditions (softfloat with DAZ/FTZ off: denormal operands raise
+ * nothing and are exact):
+ *   add, sub, sqrt      result exponent field not all ones (no NaN, inf,
+ *                       overflow); a tiny add/sub result is exact and
+ *                       sqrt cannot underflow
+ *   mul, div, cvtsd2ss  result exponent field in [2, max - 1], or an exact
+ *                       zero because a factor / the dividend is zero: no
+ *                       NaN, inf, overflow, division by zero, underflow
+ *                       (whether tininess is detected before or after
+ *                       rounding)
+ *   rcp, rsqrt          normal result (the helpers restore the flags)
+ *   min, max, cmp,      no NaN operand; the compare is done on integer
+ *   comis, ucomis       keys (-0 folded to +0)
+ *   cvt to int32        |x| < 2^31 - 1/2 (no overflow with either
+ *                       rounding; softfloat gives 0x80000000 + IE)
+ *   cvt from int32      always (PE only)
+ *   cvtss2sd            result not NaN/inf (an SNaN input raises IE)
+ * Everything else takes the slow block of the instruction: the helper
+ * and a TB exit to the next instruction, as x87-shadow does. Legacy SSE
+ * encodings only (VEX forms take the helpers). The slow blocks share the
+ * X87SlowBlock array and emitter.
+ *
+ * SPDX-License-Identifier: GPL-2.0-or-later
+ */
+
+#define W 32
+#include "sse-fast-lane.c.inc"
+#define W 64
+#include "sse-fast-lane.c.inc"
+
+/* room for the instruction's slow block and the guard */
+static bool sses_admissible(DisasContext *s)
+{
+    return s->sses_on && !(s->prefix & PREFIX_VEX)
+        && s->x87s_nslow + 2 <= X87S_MAX_SLOW;
+}
+
+/* t = env->sse_fast_mode & PE sticky: nonzero when the inline path applies now */
+static void sses_state(DisasContext *s, TCGv_i32 t)
+{
+    TCGv_i32 f = x87s_tmp_i32(s);
+
+    tcg_gen_ld8u_i32(t, tcg_env, offsetof(CPUX86State, sse_fast_mode));
+    tcg_gen_ld16u_i32(f, tcg_env,
+                      offsetof(CPUX86State, sse_status.float_exception_flags));
+    tcg_gen_shri_i32(f, f, ctz32(float_flag_inexact));
+    tcg_gen_and_i32(t, t, f);
+}
+
+/* Runtime guard, once per TB (see the header). */
+static void sses_guard(DisasContext *s)
+{
+    X87SlowBlock *b;
+    TCGv_i32 t;
+
+    if (s->sses_guarded) {
+        return;
+    }
+    s->sses_guarded = true;
+    b = x87s_new_slow(s, SSES_SLOW_GUARD);
+    t = x87s_tmp_i32(s);
+    sses_state(s, t);
+    tcg_gen_brcondi_i32(TCG_COND_EQ, t, 0, b->label);
+}
+
+/*
+ * After a helper that may raise PE, in a TB translated without the fast
+ * flag: leave the TB if the fast path now applies, so the loop this
+ * instruction sits in gets retranslated inline instead of running the
+ * helpers until the next lookup. The check is emitted at the end of the
+ * instruction (sses_emit_pending, from the decoder), after its register
+ * and flags write-back.
+ */
+static void sses_helper_done(DisasContext *s)
+{
+    if (s->sses_on || !tcg_f64_supported()
+        || (s->flags & (HF_TF_MASK | HF_INHIBIT_IRQ_MASK))
+        || s->x87s_nslow + 1 > X87S_MAX_SLOW) {
+        return;
+    }
+    s->sses_pending = true;
+}
+
+static void sses_emit_pending(DisasContext *s)
+{
+    X87SlowBlock *b;
+    TCGv_i32 t;
+
+    if (!s->sses_pending) {
+        return;
+    }
+    s->sses_pending = false;
+    b = x87s_new_slow(s, SSES_SLOW_EXIT_NEXT);
+    t = x87s_tmp_i32(s);
+    sses_state(s, t);
+    tcg_gen_brcondi_i32(TCG_COND_NE, t, 0, b->label);
+    x87s_free_tmps(s);
+}
+
+/* MXCSR (or the flags) may change: the fast flag is a TB flag */
+static void sses_end_tb(DisasContext *s)
+{
+    if (tcg_f64_supported()) {
+        s->base.is_jmp = DISAS_EOB_NEXT;
+    }
+}
+
+static X87SlowBlock *sses_slow(DisasContext *s, X86DecodedInsn *decode,
+                               int kind, void *fn, int nargs)
+{
+    X87SlowBlock *b = x87s_new_slow(s, kind);
+    int i;
+
+    b->fn = fn;
+    b->nargs = nargs;
+    for (i = 0; i < 3; i++) {
+        X86DecodedOp *op = &decode->op[i];
+
+        b->off[i] = op->unit == X86_OP_SSE ? vector_reg_offset(op) : 0;
+    }
+    return b;
+}
+
+static TCGv_ptr sses_ptr(DisasContext *s, X87SlowBlock *b, int i)
+{
+    TCGv_ptr p = x87s_tmp_ptr(s);
+
+    tcg_gen_addi_ptr(p, tcg_env, b->off[i]);
+    return p;
+}
+
+static void sses_count(DisasContext *s, int idx)
+{
+    TCGv_i64 t = x87s_tmp_i64(s);
+    int off = offsetof(CPUX86State, sses_stat[idx]);
+
+    tcg_gen_ld_i64(t, tcg_env, off);
+    tcg_gen_addi_i64(t, t, 1);
+    tcg_gen_st_i64(t, tcg_env, off);
+}
+
+/* Called by x87s_emit_slow_blocks with the translator state restored. */
+static void sses_emit_slow_block(DisasContext *s, X87SlowBlock *b)
+{
+    x87s_flush_code(s);
+    sses_count(s, b->kind == SSES_SLOW_GUARD ? 0 : b->kind == SSES_SLOW_EXIT_NEXT ? 1
+               : b->kind == SSES_SLOW_HELPER ? 2 : 3);
+    switch (b->kind) {
+    case SSES_SLOW_GUARD:
+        x87s_exit_to(s, b->pc_insn);
+        return;
+    case SSES_SLOW_EXIT_NEXT:
+        break;
+    case SSES_SLOW_HELPER:
+        if (b->nargs == 3) {
+            ((SSEFunc_0_eppp)b->fn)(tcg_env, sses_ptr(s, b, 0),
+                                    sses_ptr(s, b, 1), sses_ptr(s, b, 2));
+        } else {
+            ((SSEFunc_0_epp)b->fn)(tcg_env, sses_ptr(s, b, 0),
+                                   sses_ptr(s, b, 2));
+        }
+        break;
+    case SSES_SLOW_CVT2SI: {
+        TCGv_i32 r = x87s_tmp_i32(s);
+        TCGv t = tcg_temp_new();
+
+        ((SSEFunc_i_ep)b->fn)(r, tcg_env, sses_ptr(s, b, 2));
+        tcg_gen_extu_i32_tl(t, r);
+        gen_op_mov_reg_v(s, b->reg_ot, b->reg_n, t);
+        break;
+    }
+    case SSES_SLOW_COMIS:
+        ((SSEFunc_0_epp)b->fn)(tcg_env, sses_ptr(s, b, 1), sses_ptr(s, b, 2));
+        assume_cc_op(s, CC_OP_EFLAGS);
+        break;
+    default:
+        g_assert_not_reached();
+    }
+    x87s_exit_to(s, b->pc_next);
+}
+
+/* ------------------------------------------------------------------ */
+/* vector path: arithmetic, min/max and compares, all lanes or lane 0    */
+
+typedef struct SSESVec {
+    TCGType type;
+    unsigned vece;
+    uint64_t nan_limit;         /* x << 1 above this: NaN; at or above: exp all ones */
+    uint64_t exp_mask, exp_lo, exp_range;   /* fields 2 .. max - 1 */
+    uint64_t one;
+    TCGv_vec tmps[24];
+    int ntmps;
+} SSESVec;
+
+static void sses_vinit(SSESVec *v, bool dbl)
+{
+    v->type = TCG_TYPE_V128;
+    v->ntmps = 0;
+    if (dbl) {
+        v->vece = MO_64;
+        v->nan_limit = 0xffe0000000000000ull;
+        v->exp_mask = 0x7ff0000000000000ull;
+        v->exp_lo = 0x0020000000000000ull;
+        v->exp_range = 0x7fd0000000000000ull;
+        v->one = 0x3ff0000000000000ull;
+    } else {
+        v->vece = MO_32;
+        v->nan_limit = 0xff000000u;
+        v->exp_mask = 0x7f800000u;
+        v->exp_lo = 0x01000000u;
+        v->exp_range = 0x7e800000u;
+        v->one = 0x3f800000u;
+    }
+}
+
+static TCGv_vec sses_vtmp(SSESVec *v)
+{
+    TCGv_vec t = tcg_temp_new_vec(v->type);
+
+    tcg_debug_assert(v->ntmps < ARRAY_SIZE(v->tmps));
+    v->tmps[v->ntmps++] = t;
+    return t;
+}
+
+static void sses_vfree(SSESVec *v)
+{
+    int i;
+
+    for (i = 0; i < v->ntmps; i++) {
+        tcg_temp_free_vec(v->tmps[i]);
+    }
+    v->ntmps = 0;
+}
+
+static TCGv_vec sses_vconst(SSESVec *v, uint64_t c)
+{
+    return tcg_constant_vec(v->type, v->vece, c);
+}
+
+/*
+ * Lanes whose result is acceptable, all ones. `pz`: lanes where an exact
+ * zero result is allowed (a factor / the dividend is zero), or NULL.
+ */
+static TCGv_vec sses_vcheck(SSESVec *v, TCGv_vec r, TCGv_vec pz, bool normal_only)
+{
+    unsigned vece = v->vece;
+    TCGv_vec ok = sses_vtmp(v);
+    TCGv_vec t = sses_vtmp(v);
+
+    if (!normal_only) {
+        tcg_gen_shli_vec(vece, t, r, 1);
+        tcg_gen_cmp_vec(TCG_COND_LTU, vece, ok, t, sses_vconst(v, v->nan_limit));
+    } else {
+        TCGv_vec e = sses_vtmp(v);
+
+        tcg_gen_and_vec(vece, e, r, sses_vconst(v, v->exp_mask));
+        tcg_gen_sub_vec(vece, t, e, sses_vconst(v, v->exp_lo));
+        tcg_gen_cmp_vec(TCG_COND_LTU, vece, ok, t, sses_vconst(v, v->exp_range));
+        if (pz) {
+            tcg_gen_cmp_vec(TCG_COND_NE, vece, t, e, sses_vconst(v, v->exp_mask));
+            tcg_gen_and_vec(vece, t, t, pz);
+            tcg_gen_or_vec(vece, ok, ok, t);
+        }
+    }
+    return ok;
+}
+
+/* lanes where x is zero (either sign), all ones; t = x << 1 out */
+static TCGv_vec sses_vzero(SSESVec *v, TCGv_vec x, TCGv_vec *tp)
+{
+    TCGv_vec t = sses_vtmp(v);
+    TCGv_vec z = sses_vtmp(v);
+
+    tcg_gen_shli_vec(v->vece, t, x, 1);
+    tcg_gen_cmp_vec(TCG_COND_EQ, v->vece, z, t, sses_vconst(v, 0));
+    if (tp) {
+        *tp = t;
+    }
+    return z;
+}
+
+/* total-order key of a non-NaN lane: -0 folded to +0, negatives flipped */
+static TCGv_vec sses_vkey(SSESVec *v, TCGv_vec x, TCGv_vec z)
+{
+    unsigned vece = v->vece;
+    TCGv_vec c = sses_vtmp(v);
+    TCGv_vec sgn = sses_vtmp(v);
+    TCGv_vec k = sses_vtmp(v);
+
+    tcg_gen_andc_vec(vece, c, x, z);
+    tcg_gen_sari_vec(vece, sgn, c, (8 << vece) - 1);
+    tcg_gen_shri_vec(vece, sgn, sgn, 1);
+    tcg_gen_xor_vec(vece, k, c, sgn);
+    return k;
+}
+
+/* ok = lanes with no NaN operand; keys of both operands */
+static TCGv_vec sses_vkeys(SSESVec *v, TCGv_vec a, TCGv_vec b,
+                           TCGv_vec *ka, TCGv_vec *kb)
+{
+    unsigned vece = v->vece;
+    TCGv_vec ta, tb;
+    TCGv_vec za = sses_vzero(v, a, &ta);
+    TCGv_vec zb = sses_vzero(v, b, &tb);
+    TCGv_vec na = sses_vtmp(v);
+    TCGv_vec nb = sses_vtmp(v);
+    TCGv_vec ok = sses_vtmp(v);
+
+    tcg_gen_cmp_vec(TCG_COND_GTU, vece, na, ta, sses_vconst(v, v->nan_limit));
+    tcg_gen_cmp_vec(TCG_COND_GTU, vece, nb, tb, sses_vconst(v, v->nan_limit));
+    tcg_gen_or_vec(vece, ok, na, nb);
+    tcg_gen_not_vec(vece, ok, ok);
+    *ka = sses_vkey(v, a, za);
+    *kb = sses_vkey(v, b, zb);
+    return ok;
+}
+
+/*
+ * One packed arithmetic, min/max or compare instruction on the vector
+ * unit: op[0] = op[1] op op[2] (unary: op[2]). Every lane is computed
+ * and checked; the lane mask goes through env->sses_scratch (TCG has no
+ * vector-to-scalar move), then one branch to the slow block, then the
+ * store: nothing is written before the check. Scalar forms use the
+ * general-register lane code (sse-fast-lane.c.inc).
+ */
+static void sses_vec_insn(DisasContext *s, X86DecodedInsn *decode, int op,
+                          int pred, bool dbl, TCGLabel *slow)
+{
+    static const TCGCond conds[8] = {
+        TCG_COND_EQ, TCG_COND_LT, TCG_COND_LE, TCG_COND_NEVER,
+        TCG_COND_NE, TCG_COND_GE, TCG_COND_GT, TCG_COND_ALWAYS,
+    };
+    SSESVec vv, *v = &vv;
+    unsigned vece;
+    int d = vector_reg_offset(&decode->op[0]);
+    int src = vector_reg_offset(&decode->op[2]);
+    bool unary = op == SSES_SQRT || op == SSES_RCP || op == SSES_RSQRT;
+    int scratch = offsetof(CPUX86State, sses_scratch[0]);
+    TCGv_vec a = NULL, b, r, ok, t, pz, ka, kb;
+    TCGv_i64 lo, hi;
+
+    sses_vinit(v, dbl);
+    vece = v->vece;
+    b = sses_vtmp(v);
+    tcg_gen_ld_vec(b, tcg_env, src);
+    if (!unary) {
+        a = sses_vtmp(v);
+        tcg_gen_ld_vec(a, tcg_env, vector_reg_offset(&decode->op[1]));
+    }
+    r = sses_vtmp(v);
+    switch (op) {
+    case SSES_ADD:
+        tcg_gen_fadd_vec(vece, r, a, b);
+        ok = sses_vcheck(v, r, NULL, false);
+        break;
+    case SSES_SUB:
+        tcg_gen_fsub_vec(vece, r, a, b);
+        ok = sses_vcheck(v, r, NULL, false);
+        break;
+    case SSES_SQRT:
+        tcg_gen_fsqrt_vec(vece, r, b);
+        ok = sses_vcheck(v, r, NULL, false);
+        break;
+    case SSES_MUL:
+        tcg_gen_fmul_vec(vece, r, a, b);
+        pz = sses_vzero(v, a, NULL);
+        t = sses_vzero(v, b, NULL);
+        tcg_gen_or_vec(vece, pz, pz, t);
+        ok = sses_vcheck(v, r, pz, true);
+        break;
+    case SSES_DIV:
+        tcg_gen_fdiv_vec(vece, r, a, b);
+        pz = sses_vzero(v, a, NULL);
+        ok = sses_vcheck(v, r, pz, true);
+        break;
+    case SSES_RCP:
+        tcg_gen_fdiv_vec(vece, r, sses_vconst(v, v->one), b);
+        ok = sses_vcheck(v, r, NULL, true);
+        break;
+    case SSES_RSQRT:
+        t = sses_vtmp(v);
+        tcg_gen_fsqrt_vec(vece, t, b);
+        tcg_gen_fdiv_vec(vece, r, sses_vconst(v, v->one), t);
+        ok = sses_vcheck(v, r, NULL, true);
+        break;
+    case SSES_MIN:
+    case SSES_MAX:
+        ok = sses_vkeys(v, a, b, &ka, &kb);
+        t = sses_vtmp(v);
+        if (op == SSES_MAX) {
+            tcg_gen_cmp_vec(TCG_COND_LT, vece, t, kb, ka);
+        } else {
+            tcg_gen_cmp_vec(TCG_COND_LT, vece, t, ka, kb);
+        }
+        tcg_gen_bitsel_vec(vece, r, t, a, b);
+        break;
+    case SSES_CMP:
+        ok = sses_vkeys(v, a, b, &ka, &kb);
+        if (pred == 3) {
+            tcg_gen_dupi_vec(vece, r, 0);
+        } else if (pred == 7) {
+            tcg_gen_dupi_vec(vece, r, -1);
+        } else {
+            tcg_gen_cmp_vec(conds[pred], vece, r, ka, kb);
+        }
+        break;
+    default:
+        g_assert_not_reached();
+    }
+
+    tcg_gen_st_vec(ok, tcg_env, scratch);
+    lo = x87s_tmp_i64(s);
+    hi = x87s_tmp_i64(s);
+    tcg_gen_ld_i64(lo, tcg_env, scratch);
+    tcg_gen_ld_i64(hi, tcg_env, scratch + 8);
+    tcg_gen_and_i64(lo, lo, hi);
+    tcg_gen_brcondi_i64(TCG_COND_NE, lo, -1, slow);
+    tcg_gen_st_vec(r, tcg_env, d);
+    sses_vfree(v);
+}
+
+/* ------------------------------------------------------------------ */
+/* instructions (return false: not inlined, emit the helper)            */
+
+/*
+ * add/sub/mul/div/min/max (pd, ps, sd, ss), sqrt (all four), rcp and
+ * rsqrt (ps, ss). The helper pointers are those the caller would use.
+ */
+static bool sses_arith(DisasContext *s, X86DecodedInsn *decode, int op,
+                       void *pd, void *ps, void *sd, void *ss)
+{
+    bool scalar = (s->prefix & (PREFIX_REPZ | PREFIX_REPNZ)) != 0;
+    bool dbl = (s->prefix & (PREFIX_DATA | PREFIX_REPNZ)) != 0;
+    bool unary = op == SSES_SQRT || op == SSES_RCP || op == SSES_RSQRT;
+    void *fn = scalar ? (dbl ? sd : ss) : (dbl ? pd : ps);
+    X87SlowBlock *b;
+
+    if (!fn || !sses_admissible(s) || !tcg_fp_vec_supported(dbl ? MO_64 : MO_32)
+        || (scalar && decode->op[0].offset != decode->op[1].offset)) {
+        return false;
+    }
+    b = sses_slow(s, decode, SSES_SLOW_HELPER, fn, unary && !scalar ? 2 : 3);
+    sses_guard(s);
+    if (!scalar) {
+        sses_vec_insn(s, decode, op, 0, dbl, b->label);
+    } else if (dbl) {
+        sses_scalar_64(s, decode, op, 0, b->label);
+    } else {
+        sses_scalar_32(s, decode, op, 0, b->label);
+    }
+    x87s_free_tmps(s);
+    return true;
+}
+
+/* cmpps/pd/ss/sd with a legacy predicate (immediate & 7) */
+static bool sses_cmp(DisasContext *s, X86DecodedInsn *decode, void *fn)
+{
+    bool scalar = (s->prefix & (PREFIX_REPZ | PREFIX_REPNZ)) != 0;
+    bool dbl = (s->prefix & (PREFIX_DATA | PREFIX_REPNZ)) != 0;
+    X87SlowBlock *b;
+
+    if (!sses_admissible(s) || !tcg_fp_vec_supported(dbl ? MO_64 : MO_32)
+        || (scalar && decode->op[0].offset != decode->op[1].offset)) {
+        return false;
+    }
+    b = sses_slow(s, decode, SSES_SLOW_HELPER, fn, 3);
+    sses_guard(s);
+    if (!scalar) {
+        sses_vec_insn(s, decode, SSES_CMP, decode->immediate & 7, dbl, b->label);
+    } else if (dbl) {
+        sses_scalar_64(s, decode, SSES_CMP, decode->immediate & 7, b->label);
+    } else {
+        sses_scalar_32(s, decode, SSES_CMP, decode->immediate & 7, b->label);
+    }
+    x87s_free_tmps(s);
+    return true;
+}
+
+/* comiss/comisd/ucomiss/ucomisd: op[1] vs op[2] -> eflags */
+static bool sses_comis(DisasContext *s, X86DecodedInsn *decode, void *fn)
+{
+    bool dbl = (s->prefix & PREFIX_DATA) != 0;
+    int v = vector_reg_offset(&decode->op[1]);
+    int src = vector_reg_offset(&decode->op[2]);
+    TCGv_i32 cc;
+    X87SlowBlock *b;
+
+    if (!sses_admissible(s)) {
+        return false;
+    }
+    cc = x87s_tmp_i32(s);
+    b = sses_slow(s, decode, SSES_SLOW_COMIS, fn, 2);
+    sses_guard(s);
+    if (dbl) {
+        TCGv_i64 a = x87s_tmp_i64(s);
+        TCGv_i64 c = x87s_tmp_i64(s);
+
+        tcg_gen_ld_i64(a, tcg_env, v + offsetof(ZMMReg, ZMM_Q(0)));
+        tcg_gen_ld_i64(c, tcg_env, src + offsetof(ZMMReg, ZMM_Q(0)));
+        sses_comis_64(s, cc, a, c, b->label);
+    } else {
+        TCGv_i32 a = x87s_tmp_i32(s);
+        TCGv_i32 c = x87s_tmp_i32(s);
+
+        tcg_gen_ld_i32(a, tcg_env, v + offsetof(ZMMReg, ZMM_L(0)));
+        tcg_gen_ld_i32(c, tcg_env, src + offsetof(ZMMReg, ZMM_L(0)));
+        sses_comis_32(s, cc, a, c, b->label);
+    }
+    tcg_gen_extu_i32_tl(cpu_cc_src, cc);
+    set_cc_op(s, CC_OP_EFLAGS);
+    x87s_free_tmps(s);
+    return true;
+}
+
+/* cvtsi2ss/sd from a 32-bit integer: only PE can be raised, no slow block */
+static bool sses_cvtsi2s(DisasContext *s, X86DecodedInsn *decode, TCGv_i32 in)
+{
+    int d = vector_reg_offset(&decode->op[0]);
+
+    if (!sses_admissible(s)) {
+        return false;
+    }
+    sses_guard(s);
+    if (s->prefix & PREFIX_REPNZ) {
+        TCGv_i64 r = x87s_tmp_i64(s);
+
+        tcg_gen_cvt_i32_f64(r, in);
+        tcg_gen_st_i64(r, tcg_env, d + offsetof(ZMMReg, ZMM_Q(0)));
+    } else {
+        TCGv_i32 r = x87s_tmp_i32(s);
+
+        tcg_gen_cvt_i32_f32(r, in);
+        tcg_gen_st_i32(r, tcg_env, d + offsetof(ZMMReg, ZMM_L(0)));
+    }
+    x87s_free_tmps(s);
+    return true;
+}
+
+/* cvt(t)ss2si / cvt(t)sd2si to a 32-bit register (s->T0) */
+static bool sses_cvts2si(DisasContext *s, X86DecodedInsn *decode, bool trunc,
+                         void *ss2si, void *sd2si)
+{
+    bool dbl = (s->prefix & PREFIX_REPNZ) != 0;
+    int src = vector_reg_offset(&decode->op[2]);
+    TCGv_i32 r;
+    X87SlowBlock *b;
+
+    if (!sses_admissible(s) || decode->op[0].unit != X86_OP_INT) {
+        return false;
+    }
+    r = x87s_tmp_i32(s);
+    b = sses_slow(s, decode, SSES_SLOW_CVT2SI, dbl ? sd2si : ss2si, 0);
+    b->reg_ot = decode->op[0].ot;
+    b->reg_n = decode->op[0].n;
+    sses_guard(s);
+    if (dbl) {
+        TCGv_i64 a = x87s_tmp_i64(s);
+        TCGv_i64 t = x87s_tmp_i64(s);
+
+        tcg_gen_ld_i64(a, tcg_env, src + offsetof(ZMMReg, ZMM_Q(0)));
+        tcg_gen_shli_i64(t, a, 1);
+        /* |a| < 2^31 - 1/2 (0x41dfffffffe00000), also excludes NaN */
+        tcg_gen_brcondi_i64(TCG_COND_GEU, t, 0x83bfffffffc00000ull, b->label);
+        if (trunc) {
+            tcg_gen_cvtt_f64_i32(r, a);
+        } else {
+            tcg_gen_cvt_f64_i32(r, a);
+        }
+    } else {
+        TCGv_i32 a = x87s_tmp_i32(s);
+        TCGv_i32 t = x87s_tmp_i32(s);
+
+        tcg_gen_ld_i32(a, tcg_env, src + offsetof(ZMMReg, ZMM_L(0)));
+        tcg_gen_shli_i32(t, a, 1);
+        /* |a| < 2^31 (0x4f000000); floats there are integers already */
+        tcg_gen_brcondi_i32(TCG_COND_GEU, t, 0x9e000000u, b->label);
+        if (trunc) {
+            tcg_gen_cvtt_f32_i32(r, a);
+        } else {
+            tcg_gen_cvt_f32_i32(r, a);
+        }
+    }
+    tcg_gen_extu_i32_tl(s->T0, r);
+    x87s_free_tmps(s);
+    return true;
+}
+
+/* cvtss2sd (to_double) and cvtsd2ss, lane 0 of op[2] into lane 0 of op[0] */
+static bool sses_cvt_ss_sd(DisasContext *s, X86DecodedInsn *decode,
+                           bool to_double, void *fn)
+{
+    int d = vector_reg_offset(&decode->op[0]);
+    int src = vector_reg_offset(&decode->op[2]);
+    X87SlowBlock *b;
+
+    if (!sses_admissible(s)
+        || decode->op[0].offset != decode->op[1].offset) {
+        return false;
+    }
+    b = sses_slow(s, decode, SSES_SLOW_HELPER, fn, 3);
+    sses_guard(s);
+    if (to_double) {
+        TCGv_i32 a = x87s_tmp_i32(s);
+        TCGv_i64 a64 = x87s_tmp_i64(s);
+        TCGv_i64 r = x87s_tmp_i64(s);
+        TCGv_i64 t = x87s_tmp_i64(s);
+
+        tcg_gen_ld_i32(a, tcg_env, src + offsetof(ZMMReg, ZMM_L(0)));
+        tcg_gen_extu_i32_i64(a64, a);
+        tcg_gen_cvt_f32_f64(r, a64);
+        tcg_gen_shli_i64(t, r, 1);
+        tcg_gen_brcondi_i64(TCG_COND_GEU, t, 0xffe0000000000000ull, b->label);
+        tcg_gen_st_i64(r, tcg_env, d + offsetof(ZMMReg, ZMM_Q(0)));
+    } else {
+        TCGv_i64 a = x87s_tmp_i64(s);
+        TCGv_i64 r64 = x87s_tmp_i64(s);
+        TCGv_i64 z64 = x87s_tmp_i64(s);
+        TCGv_i32 r = x87s_tmp_i32(s);
+        TCGv_i32 z = x87s_tmp_i32(s);
+        TCGv_i32 ok;
+
+        tcg_gen_ld_i64(a, tcg_env, src + offsetof(ZMMReg, ZMM_Q(0)));
+        tcg_gen_cvt_f64_f32(r64, a);
+        tcg_gen_extrl_i64_i32(r, r64);
+        tcg_gen_shli_i64(z64, a, 1);            /* zero iff a is zero */
+        tcg_gen_setcondi_i64(TCG_COND_NE, z64, z64, 0);
+        tcg_gen_extrl_i64_i32(z, z64);
+        ok = sses_check_32(s, NULL, r, z, true);
+        tcg_gen_brcondi_i32(TCG_COND_EQ, ok, 0, b->label);
+        tcg_gen_st_i32(r, tcg_env, d + offsetof(ZMMReg, ZMM_L(0)));
+    }
+    x87s_free_tmps(s);
+    return true;
+}
--- /dev/null
+++ b/target/i386/tcg/sse-fast-lane.c.inc
@@ -0,0 +1,259 @@
+/*
+ * SSE inline fast path, per-lane code for one element width.
+ * Included twice from sse-fast.c.inc with W = 32 and W = 64. The
+ * conditions are documented there.
+ *
+ * SPDX-License-Identifier: GPL-2.0-or-later
+ */
+
+#define TCGv_iW         glue(TCGv_i, W)
+#define TMP()           glue(x87s_tmp_i, W)(s)
+#define OPW(name)       glue(glue(tcg_gen_, name), glue(_i, W))
+#define FPW(name)       glue(glue(tcg_gen_, name), glue(_f, W))
+#define CONSTW(v)       glue(tcg_constant_i, W)(v)
+#define LANEFN(name)    glue(glue(sses_, name), glue(_, W))
+
+#if W == 32
+#define LANE(i)         offsetof(ZMMReg, ZMM_L(i))
+#define NAN_LIMIT       0xff000000u             /* x << 1: >= exp all ones, > NaN */
+#define EXP_MASK        0x7f800000u
+#define EXP_LO          0x01000000u             /* exponent field 2 */
+#define EXP_RANGE       0x7e800000u             /* fields 2 .. 254 */
+#define ONE_BITS        0x3f800000u
+#define TO_I32(d, x)    tcg_gen_mov_i32(d, x)
+#else
+#define LANE(i)         offsetof(ZMMReg, ZMM_Q(i))
+#define NAN_LIMIT       0xffe0000000000000ull
+#define EXP_MASK        0x7ff0000000000000ull
+#define EXP_LO          0x0020000000000000ull
+#define EXP_RANGE       0x7fd0000000000000ull   /* fields 2 .. 2046 */
+#define ONE_BITS        0x3ff0000000000000ull
+#define TO_I32(d, x)    tcg_gen_extrl_i64_i32(d, x)
+#endif
+
+/*
+ * ok &= "r is an acceptable fast-path result". `z` is nonzero unless the
+ * result is an exact zero for the reason the fast path allows (an operand
+ * of a multiply, the dividend of a divide, is zero); NULL: no such case.
+ */
+static TCGv_iW LANEFN(check)(DisasContext *s, TCGv_iW ok, TCGv_iW r,
+                             TCGv_iW z, bool normal_only)
+{
+    TCGv_iW e = TMP();
+    TCGv_iW okl = TMP();
+
+    if (!normal_only) {
+        /* exponent field not all ones: no NaN, infinity or overflow */
+        OPW(shli)(e, r, 1);
+        OPW(setcondi)(TCG_COND_LTU, okl, e, NAN_LIMIT);
+    } else {
+        /* exponent field in [2, max - 1], or an allowed exact zero */
+        TCGv_iW t = TMP();
+        TCGv_iW pz = TMP();
+
+        OPW(andi)(e, r, EXP_MASK);
+        OPW(subi)(t, e, EXP_LO);
+        OPW(setcondi)(TCG_COND_LTU, okl, t, EXP_RANGE);
+        if (z) {
+            OPW(setcondi)(TCG_COND_EQ, pz, z, 0);
+            OPW(setcondi)(TCG_COND_NE, t, e, EXP_MASK);   /* not 0 * inf */
+            OPW(and)(pz, pz, t);
+            OPW(or)(okl, okl, pz);
+        }
+    }
+    if (ok) {
+        OPW(and)(ok, ok, okl);
+        return ok;
+    }
+    return okl;
+}
+
+/* r = a op b (unary ops: b), see the table in sse-fast.c.inc */
+static TCGv_iW LANEFN(arith)(DisasContext *s, int op, TCGv_iW r,
+                             TCGv_iW a, TCGv_iW b, TCGv_iW ok)
+{
+    TCGv_iW z = TMP();
+    TCGv_iW t;
+
+    switch (op) {
+    case SSES_ADD:
+        FPW(add)(r, a, b);
+        return LANEFN(check)(s, ok, r, NULL, false);
+    case SSES_SUB:
+        FPW(sub)(r, a, b);
+        return LANEFN(check)(s, ok, r, NULL, false);
+    case SSES_SQRT:
+        FPW(sqrt)(r, b);
+        return LANEFN(check)(s, ok, r, NULL, false);
+    case SSES_MUL:
+        FPW(mul)(r, a, b);
+        t = TMP();
+        OPW(shli)(z, a, 1);
+        OPW(shli)(t, b, 1);
+        OPW(umin)(z, z, t);                     /* zero iff a or b is zero */
+        return LANEFN(check)(s, ok, r, z, true);
+    case SSES_DIV:
+        FPW(div)(r, a, b);
+        OPW(shli)(z, a, 1);                     /* zero iff the dividend is zero */
+        return LANEFN(check)(s, ok, r, z, true);
+    case SSES_RCP:
+        FPW(div)(r, CONSTW(ONE_BITS), b);
+        return LANEFN(check)(s, ok, r, NULL, true);
+    case SSES_RSQRT:
+        t = TMP();
+        FPW(sqrt)(t, b);
+        FPW(div)(r, CONSTW(ONE_BITS), t);
+        return LANEFN(check)(s, ok, r, NULL, true);
+    default:
+        g_assert_not_reached();
+    }
+}
+
+/* total-order key of a non-NaN value (t = x << 1): -0 -> +0, negatives flipped */
+static void LANEFN(key)(DisasContext *s, TCGv_iW k, TCGv_iW x, TCGv_iW t)
+{
+    TCGv_iW zero = CONSTW(0);
+    TCGv_iW c = TMP();
+    TCGv_iW sgn = TMP();
+
+    OPW(movcond)(TCG_COND_EQ, c, t, zero, zero, x);
+    OPW(sari)(sgn, c, W - 1);
+    OPW(shri)(sgn, sgn, 1);
+    OPW(xor)(k, c, sgn);
+}
+
+/* ok &= neither a nor b is a NaN; ka, kb = their keys */
+static TCGv_iW LANEFN(keys)(DisasContext *s, TCGv_iW ok, TCGv_iW ka,
+                            TCGv_iW kb, TCGv_iW a, TCGv_iW b)
+{
+    TCGv_iW ta = TMP();
+    TCGv_iW tb = TMP();
+    TCGv_iW m = TMP();
+    TCGv_iW okl = TMP();
+
+    OPW(shli)(ta, a, 1);
+    OPW(shli)(tb, b, 1);
+    OPW(umax)(m, ta, tb);
+    OPW(setcondi)(TCG_COND_LEU, okl, m, NAN_LIMIT);
+    LANEFN(key)(s, ka, a, ta);
+    LANEFN(key)(s, kb, b, tb);
+    if (ok) {
+        OPW(and)(ok, ok, okl);
+        return ok;
+    }
+    return okl;
+}
+
+/* comis/ucomis: cc = CC_Z (equal) | CC_C (less), other flags clear */
+static void LANEFN(comis)(DisasContext *s, TCGv_i32 cc, TCGv_iW a,
+                          TCGv_iW b, TCGLabel *slow)
+{
+    TCGv_iW ka = TMP();
+    TCGv_iW kb = TMP();
+    TCGv_iW eq = TMP();
+    TCGv_iW lt = TMP();
+    TCGv_iW t = TMP();
+    TCGv_iW ok;
+
+    ok = LANEFN(keys)(s, NULL, ka, kb, a, b);
+    OPW(brcondi)(TCG_COND_EQ, ok, 0, slow);
+    OPW(setcond)(TCG_COND_EQ, eq, ka, kb);
+    OPW(setcond)(TCG_COND_LT, lt, ka, kb);
+    OPW(movcond)(TCG_COND_NE, t, eq, CONSTW(0), CONSTW(CC_Z), CONSTW(0));
+    OPW(movcond)(TCG_COND_NE, lt, lt, CONSTW(0), CONSTW(CC_C), CONSTW(0));
+    OPW(or)(t, t, lt);
+    TO_I32(cc, t);
+}
+
+/* min: lt(a, b) ? a : b; max: lt(b, a) ? a : b (Intel's operand order rules) */
+static TCGv_iW LANEFN(minmax)(DisasContext *s, bool max, TCGv_iW r,
+                              TCGv_iW a, TCGv_iW b, TCGv_iW ok)
+{
+    TCGv_iW ka = TMP();
+    TCGv_iW kb = TMP();
+    TCGv_iW lt = TMP();
+
+    ok = LANEFN(keys)(s, ok, ka, kb, a, b);
+    if (max) {
+        OPW(setcond)(TCG_COND_LT, lt, kb, ka);
+    } else {
+        OPW(setcond)(TCG_COND_LT, lt, ka, kb);
+    }
+    OPW(movcond)(TCG_COND_NE, r, lt, CONSTW(0), a, b);
+    return ok;
+}
+
+/* cmpps predicate 0..7: eq lt le unord neq nlt nle ord -> all-ones / 0 */
+static TCGv_iW LANEFN(cmp)(DisasContext *s, int pred, TCGv_iW r,
+                           TCGv_iW a, TCGv_iW b, TCGv_iW ok)
+{
+    static const TCGCond conds[8] = {
+        TCG_COND_EQ, TCG_COND_LT, TCG_COND_LE, TCG_COND_NEVER,
+        TCG_COND_NE, TCG_COND_GE, TCG_COND_GT, TCG_COND_ALWAYS,
+    };
+    TCGv_iW ka = TMP();
+    TCGv_iW kb = TMP();
+
+    ok = LANEFN(keys)(s, ok, ka, kb, a, b);
+    if (pred == 3) {
+        OPW(movi)(r, 0);
+    } else if (pred == 7) {
+        OPW(movi)(r, -1);
+    } else {
+        OPW(setcond)(conds[pred], r, ka, kb);
+        OPW(neg)(r, r);
+    }
+    return ok;
+}
+
+/*
+ * The scalar form of an arithmetic, min/max or compare instruction: lane
+ * 0 of op[0] = op[1] op op[2] (unary: op[2]) in general registers, one
+ * branch to the slow block before the store.
+ */
+static void LANEFN(scalar)(DisasContext *s, X86DecodedInsn *decode, int op,
+                           int pred, TCGLabel *slow)
+{
+    bool unary = op == SSES_SQRT || op == SSES_RCP || op == SSES_RSQRT;
+    int d = vector_reg_offset(&decode->op[0]);
+    int src = vector_reg_offset(&decode->op[2]);
+    TCGv_iW a = NULL;
+    TCGv_iW b = TMP();
+    TCGv_iW r = TMP();
+    TCGv_iW ok;
+
+    OPW(ld)(b, tcg_env, src + LANE(0));
+    if (!unary) {
+        a = TMP();
+        OPW(ld)(a, tcg_env, vector_reg_offset(&decode->op[1]) + LANE(0));
+    }
+    switch (op) {
+    case SSES_MIN:
+    case SSES_MAX:
+        ok = LANEFN(minmax)(s, op == SSES_MAX, r, a, b, NULL);
+        break;
+    case SSES_CMP:
+        ok = LANEFN(cmp)(s, pred, r, a, b, NULL);
+        break;
+    default:
+        ok = LANEFN(arith)(s, op, r, a, b, NULL);
+        break;
+    }
+    OPW(brcondi)(TCG_COND_EQ, ok, 0, slow);
+    OPW(st)(r, tcg_env, d + LANE(0));
+}
+
+#undef TCGv_iW
+#undef TMP
+#undef OPW
+#undef FPW
+#undef CONSTW
+#undef LANEFN
+#undef LANE
+#undef NAN_LIMIT
+#undef EXP_MASK
+#undef EXP_LO
+#undef EXP_RANGE
+#undef ONE_BITS
+#undef TO_I32
+#undef W
