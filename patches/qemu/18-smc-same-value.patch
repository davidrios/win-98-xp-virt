Self-modifying code: a store that leaves a code page's bytes unchanged
invalidates no translated block (M9 track, docs/tracks/m9-tcg-aarch64.md).
The store slow path compares the bytes about to be written with what is
there before notdirty_write; `-accel tcg,smc-same-value=off` restores the
old behaviour. See the comment on notdirty_write and the patch README row.
Test: tools/smc-guest-test.py.

diff --git a/accel/tcg/cputlb.c b/accel/tcg/cputlb.c
index b76a4ea..873ccdc 100644
--- a/accel/tcg/cputlb.c
+++ b/accel/tcg/cputlb.c
@@ -1324,14 +1324,25 @@ static bool victim_tlb_hit(CPUState *cpu, size_t mmu_idx, size_t index,
     return false;
 }
 
+/*
+ * @changed: false when the store leaves the bytes as they are.  A TB
+ * translated from those bytes stays valid, so nothing is invalidated —
+ * the self-modifying rasterizers of the software-rendered games rewrite
+ * their whole span parameter block per span and change a few bytes of it
+ * (patch 18, docs/tracks/m9-tcg-aarch64.md: 94 % of Moto Racer's 700,000
+ * code-page stores a second were such rewrites, each walking the page's
+ * TB list and retranslating what it hit).  The dirty bits are still set.
+ */
 static void notdirty_write(CPUState *cpu, vaddr mem_vaddr, unsigned size,
-                           CPUTLBEntryFull *full, uintptr_t retaddr)
+                           CPUTLBEntryFull *full, uintptr_t retaddr,
+                           bool changed)
 {
     ram_addr_t ram_addr = mem_vaddr + full->xlat_section;
 
     trace_memory_notdirty_write_access(mem_vaddr, ram_addr, size);
 
-    if (!cpu_physical_memory_get_dirty_flag(ram_addr, DIRTY_MEMORY_CODE)) {
+    if (changed &&
+        !cpu_physical_memory_get_dirty_flag(ram_addr, DIRTY_MEMORY_CODE)) {
         tb_invalidate_phys_range_fast(ram_addr, size, retaddr);
     }
 
@@ -1414,7 +1425,7 @@ int probe_access_full(CPUArchState *env, vaddr addr, int size,
     /* Handle clean RAM pages.  */
     if (unlikely(flags & TLB_NOTDIRTY)) {
         int dirtysize = size == 0 ? 1 : size;
-        notdirty_write(env_cpu(env), addr, dirtysize, *pfull, retaddr);
+        notdirty_write(env_cpu(env), addr, dirtysize, *pfull, retaddr, true);
         flags &= ~TLB_NOTDIRTY;
     }
 
@@ -1438,7 +1449,7 @@ int probe_access_full_mmu(CPUArchState *env, vaddr addr, int size,
     /* Handle clean RAM pages.  */
     if (unlikely(flags & TLB_NOTDIRTY)) {
         int dirtysize = size == 0 ? 1 : size;
-        notdirty_write(env_cpu(env), addr, dirtysize, *pfull, 0);
+        notdirty_write(env_cpu(env), addr, dirtysize, *pfull, 0, true);
         flags &= ~TLB_NOTDIRTY;
     }
 
@@ -1461,7 +1472,7 @@ int probe_access_flags(CPUArchState *env, vaddr addr, int size,
     /* Handle clean RAM pages. */
     if (unlikely(flags & TLB_NOTDIRTY)) {
         int dirtysize = size == 0 ? 1 : size;
-        notdirty_write(env_cpu(env), addr, dirtysize, full, retaddr);
+        notdirty_write(env_cpu(env), addr, dirtysize, full, retaddr, true);
         flags &= ~TLB_NOTDIRTY;
     }
 
@@ -1497,7 +1508,7 @@ void *probe_access(CPUArchState *env, vaddr addr, int size,
 
         /* Handle clean RAM pages.  */
         if (flags & TLB_NOTDIRTY) {
-            notdirty_write(env_cpu(env), addr, size, full, retaddr);
+            notdirty_write(env_cpu(env), addr, size, full, retaddr, true);
         }
     }
 
@@ -1614,8 +1625,31 @@ typedef struct MMULookupLocals {
     MMULookupPageData page[2];
     MemOp memop;
     int mmu_idx;
+    /*
+     * For a store of up to 8 bytes: the bytes about to be written, in
+     * address order (little-endian), so that a write to a page holding
+     * code can tell whether it changes anything (see notdirty_write).
+     */
+    bool st_known;
+    uint64_t st_bytes_le;
 } MMULookupLocals;
 
+/* the store's bytes for @data equal what is in memory already */
+static bool mmu_store_same(const MMULookupLocals *l,
+                           const MMULookupPageData *data)
+{
+    uint8_t buf[8];
+    int skip;
+
+    if (!l->st_known || !tcg_smc_same_value) {
+        return false;
+    }
+    /* the second page of a crossing store takes the bytes after the first */
+    skip = data == &l->page[1] ? l->page[0].size : 0;
+    stq_le_p(buf, l->st_bytes_le);
+    return memcmp(data->haddr, buf + skip, data->size) == 0;
+}
+
 /**
  * mmu_lookup1: translate one page
  * @cpu: generic cpu state
@@ -1695,7 +1729,8 @@ static bool mmu_lookup1(CPUState *cpu, MMULookupPageData *data, MemOp memop,
  * Trigger watchpoints for @data.addr:@data.size;
  * record writes to protected clean pages.
  */
-static void mmu_watch_or_dirty(CPUState *cpu, MMULookupPageData *data,
+static void mmu_watch_or_dirty(CPUState *cpu, const MMULookupLocals *l,
+                               MMULookupPageData *data,
                                MMUAccessType access_type, uintptr_t ra)
 {
     CPUTLBEntryFull *full = data->full;
@@ -1712,7 +1747,7 @@ static void mmu_watch_or_dirty(CPUState *cpu, MMULookupPageData *data,
 
     /* Note that notdirty is only set for writes. */
     if (flags & TLB_NOTDIRTY) {
-        notdirty_write(cpu, addr, size, full, ra);
+        notdirty_write(cpu, addr, size, full, ra, !mmu_store_same(l, data));
         flags &= ~TLB_NOTDIRTY;
     }
     data->flags = flags;
@@ -1738,6 +1773,9 @@ static bool mmu_lookup(CPUState *cpu, vaddr addr, MemOpIdx oi,
 
     l->memop = get_memop(oi);
     l->mmu_idx = get_mmuidx(oi);
+    if (type != MMU_DATA_STORE) {
+        l->st_known = false;
+    }
 
     tcg_debug_assert(l->mmu_idx < NB_MMU_MODES);
 
@@ -1752,7 +1790,7 @@ static bool mmu_lookup(CPUState *cpu, vaddr addr, MemOpIdx oi,
 
         flags = l->page[0].flags;
         if (unlikely(flags & (TLB_WATCHPOINT | TLB_NOTDIRTY))) {
-            mmu_watch_or_dirty(cpu, &l->page[0], type, ra);
+            mmu_watch_or_dirty(cpu, l, &l->page[0], type, ra);
         }
         if (unlikely(flags & TLB_BSWAP)) {
             l->memop ^= MO_BSWAP;
@@ -1775,8 +1813,8 @@ static bool mmu_lookup(CPUState *cpu, vaddr addr, MemOpIdx oi,
 
         flags = l->page[0].flags | l->page[1].flags;
         if (unlikely(flags & (TLB_WATCHPOINT | TLB_NOTDIRTY))) {
-            mmu_watch_or_dirty(cpu, &l->page[0], type, ra);
-            mmu_watch_or_dirty(cpu, &l->page[1], type, ra);
+            mmu_watch_or_dirty(cpu, l, &l->page[0], type, ra);
+            mmu_watch_or_dirty(cpu, l, &l->page[1], type, ra);
         }
 
         /*
@@ -1875,7 +1913,7 @@ static void *atomic_mmu_lookup(CPUState *cpu, vaddr addr, MemOpIdx oi,
     full = &cpu->neg.tlb.d[mmu_idx].fulltlb[index];
 
     if (unlikely(tlb_addr & TLB_NOTDIRTY)) {
-        notdirty_write(cpu, addr, size, full, retaddr);
+        notdirty_write(cpu, addr, size, full, retaddr, true);
     }
 
     if (unlikely(tlb_addr & TLB_FORCE_SLOW)) {
@@ -2729,6 +2767,8 @@ static void do_st1_mmu(CPUState *cpu, vaddr addr, uint8_t val,
     bool crosspage;
 
     cpu_req_mo(TCG_MO_LD_ST | TCG_MO_ST_ST);
+    l.st_known = true;
+    l.st_bytes_le = val;
     crosspage = mmu_lookup(cpu, addr, oi, ra, MMU_DATA_STORE, &l);
     tcg_debug_assert(!crosspage);
 
@@ -2743,6 +2783,8 @@ static void do_st2_mmu(CPUState *cpu, vaddr addr, uint16_t val,
     uint8_t a, b;
 
     cpu_req_mo(TCG_MO_LD_ST | TCG_MO_ST_ST);
+    l.st_known = true;
+    l.st_bytes_le = (get_memop(oi) & MO_BSWAP) != MO_LE ? bswap16(val) : val;
     crosspage = mmu_lookup(cpu, addr, oi, ra, MMU_DATA_STORE, &l);
     if (likely(!crosspage)) {
         do_st_2(cpu, &l.page[0], val, l.mmu_idx, l.memop, ra);
@@ -2765,6 +2807,8 @@ static void do_st4_mmu(CPUState *cpu, vaddr addr, uint32_t val,
     bool crosspage;
 
     cpu_req_mo(TCG_MO_LD_ST | TCG_MO_ST_ST);
+    l.st_known = true;
+    l.st_bytes_le = (get_memop(oi) & MO_BSWAP) != MO_LE ? bswap32(val) : val;
     crosspage = mmu_lookup(cpu, addr, oi, ra, MMU_DATA_STORE, &l);
     if (likely(!crosspage)) {
         do_st_4(cpu, &l.page[0], val, l.mmu_idx, l.memop, ra);
@@ -2786,6 +2830,8 @@ static void do_st8_mmu(CPUState *cpu, vaddr addr, uint64_t val,
     bool crosspage;
 
     cpu_req_mo(TCG_MO_LD_ST | TCG_MO_ST_ST);
+    l.st_known = true;
+    l.st_bytes_le = (get_memop(oi) & MO_BSWAP) != MO_LE ? bswap64(val) : val;
     crosspage = mmu_lookup(cpu, addr, oi, ra, MMU_DATA_STORE, &l);
     if (likely(!crosspage)) {
         do_st_8(cpu, &l.page[0], val, l.mmu_idx, l.memop, ra);
@@ -2809,6 +2855,7 @@ static void do_st16_mmu(CPUState *cpu, vaddr addr, Int128 val,
     int first;
 
     cpu_req_mo(TCG_MO_LD_ST | TCG_MO_ST_ST);
+    l.st_known = false;
     crosspage = mmu_lookup(cpu, addr, oi, ra, MMU_DATA_STORE, &l);
     if (likely(!crosspage)) {
         if (unlikely(l.page[0].flags & TLB_MMIO)) {
diff --git a/accel/tcg/internal-common.h b/accel/tcg/internal-common.h
index a8fc3db..a7bccd6 100644
--- a/accel/tcg/internal-common.h
+++ b/accel/tcg/internal-common.h
@@ -16,6 +16,8 @@ extern int64_t max_delay;
 extern int64_t max_advance;
 
 extern bool one_insn_per_tb;
+/* a store that leaves a code page's bytes unchanged invalidates no TB (patch 18) */
+extern bool tcg_smc_same_value;
 
 /*
  * Return true if CS is not running in parallel with other cpus, either
diff --git a/accel/tcg/tcg-all.c b/accel/tcg/tcg-all.c
index 2090907..206b5e2 100644
--- a/accel/tcg/tcg-all.c
+++ b/accel/tcg/tcg-all.c
@@ -99,6 +99,7 @@ static void tcg_accel_instance_init(Object *obj)
 
 bool mttcg_enabled;
 bool one_insn_per_tb;
+bool tcg_smc_same_value = true;
 
 static int tcg_init_machine(MachineState *ms)
 {
@@ -199,6 +200,16 @@ static bool tcg_get_one_insn_per_tb(Object *obj, Error **errp)
     return s->one_insn_per_tb;
 }
 
+static bool tcg_get_smc_same_value(Object *obj, Error **errp)
+{
+    return tcg_smc_same_value;
+}
+
+static void tcg_set_smc_same_value(Object *obj, bool value, Error **errp)
+{
+    tcg_smc_same_value = value;
+}
+
 static void tcg_set_one_insn_per_tb(Object *obj, bool value, Error **errp)
 {
     TCGState *s = TCG_STATE(obj);
@@ -247,6 +258,11 @@ static void tcg_accel_class_init(ObjectClass *oc, void *data)
     object_class_property_set_description(oc, "split-wx",
         "Map jit pages into separate RW and RX regions");
 
+    object_class_property_add_bool(oc, "smc-same-value",
+        tcg_get_smc_same_value, tcg_set_smc_same_value);
+    object_class_property_set_description(oc, "smc-same-value",
+        "A store that leaves a code page's bytes unchanged invalidates no translated block");
+
     object_class_property_add_bool(oc, "one-insn-per-tb",
                                    tcg_get_one_insn_per_tb,
                                    tcg_set_one_insn_per_tb);
