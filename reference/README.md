# reference

Captures from the reference rig (design doc 09: P4 + GeForce 6200 + CRT,
dual-boot Win98/XP): CRT photo sets (with camera settings), golden ATAPI
traces, benchmark results, real-GPU screenshots. Large binaries (disc dumps,
disk images) are ignored by git — only metadata, traces, photos, and results
are committed.
