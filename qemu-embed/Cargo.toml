[package]
name = "qemu-embed"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Rust bindings for libqemu-embed (embed/libqemu_embed.h): QEMU in-process"
links = "qemu-embed-i386"
build = "build.rs"

[dependencies]
